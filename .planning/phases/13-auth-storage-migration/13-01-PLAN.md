---
phase: 13-auth-storage-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jest.config.js
  - __tests__/setup/jest.setup.js
  - supabase/migrations/20260323000009_add_firebase_uid_and_storage_buckets.sql
  - src/services/supabase/phoneAuthService.ts
  - src/services/supabase/signedUrlService.ts
  - __tests__/services/phoneAuthService.test.ts
  - __tests__/services/signedUrlService.test.ts
  - __tests__/integration/authMigration.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "sendVerificationCode calls supabase.auth.signInWithOtp with E.164 phone"
    - "verifyCode calls supabase.auth.verifyOtp with phone, token, and type 'sms'"
    - "Supabase auth error codes map to user-friendly messages"
    - "signedUrlService returns public CDN URLs for regular photos (no signing)"
    - "signedUrlService returns 5-minute signed URLs for snaps"
    - "Jest can discover and run .test.ts files"
    - "firebase_uid column exists on users table for migration lookups"
    - "Auth migration integration test scaffold exists with failing tests for the Edge Function flow"
  artifacts:
    - path: "src/services/supabase/phoneAuthService.ts"
      provides: "Supabase phone OTP auth operations"
      exports: ["sendVerificationCode", "verifyCode", "getPhoneAuthErrorMessage", "validatePhoneNumber", "signOut"]
    - path: "src/services/supabase/signedUrlService.ts"
      provides: "Public URLs for photos, signed URLs for snaps"
      exports: ["getPhotoUrl", "getSnapUrl"]
    - path: "supabase/migrations/20260323000009_add_firebase_uid_and_storage_buckets.sql"
      provides: "firebase_uid column and storage bucket RLS policies"
    - path: "jest.config.js"
      provides: "Updated test config supporting .ts files"
    - path: "__tests__/integration/authMigration.test.ts"
      provides: "Integration test scaffold for silent auth migration (RED state)"
  key_links:
    - from: "src/services/supabase/phoneAuthService.ts"
      to: "src/lib/supabase.ts"
      via: "import { supabase }"
      pattern: "import.*supabase.*from.*lib/supabase"
    - from: "src/services/supabase/signedUrlService.ts"
      to: "src/lib/supabase.ts"
      via: "import { supabase }"
      pattern: "supabase\\.storage\\.from"
---

<objective>
Set up test infrastructure for TypeScript services, create Supabase phone auth service and signed URL service, add database migration for firebase_uid column and storage bucket RLS policies, and create the auth migration integration test scaffold.

Purpose: Establishes the auth and URL foundation all other Phase 13 plans build on. Phone OTP is the entry point for all users; signed URL service is consumed by storage and feed rendering. The integration test scaffold (RED state) ensures the Edge Function (Plan 02) has tests ready before implementation.
Output: Two new Supabase service files (phoneAuthService.ts, signedUrlService.ts), updated Jest config, Supabase mocks, a database migration, and a failing integration test for auth migration.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-auth-storage-migration/13-CONTEXT.md
@.planning/phases/13-auth-storage-migration/13-RESEARCH.md
@.planning/phases/13-auth-storage-migration/13-UI-SPEC.md

<interfaces>
<!-- From src/lib/supabase.ts (Phase 12): -->
```typescript
import { createClient } from '@supabase/supabase-js';
import { Database } from '@/types/database';
export const supabase = createClient<Database>(supabaseUrl, supabaseAnonKey, { ... });
```

<!-- From src/services/firebase/phoneAuthService.js (current export signatures to preserve): -->
```javascript
export const getPhoneAuthErrorMessage = (errorCode) => { ... };
export const validatePhoneNumber = (phoneNumber, countryCode) => { ... };
export const sendVerificationCode = async (phoneNumber, countryCode) => { ... };
export const verifyCode = async (confirmation, code) => { ... };
export const getCurrentUser = () => { ... };
export const signOut = async () => { ... };
export const onAuthStateChanged = (callback) => { ... };
```

<!-- From src/services/firebase/signedUrlService.js (current exports): -->
```javascript
export const getSignedPhotoUrl = async (photoPath) => { ... };
export const convertToSignedUrl = async (urlOrPath) => { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jest TS config + Supabase mocks + database migration</name>
  <files>jest.config.js, __tests__/setup/jest.setup.js, supabase/migrations/20260323000009_add_firebase_uid_and_storage_buckets.sql</files>
  <read_first>
    - jest.config.js (current config to update)
    - __tests__/setup/jest.setup.js (current Firebase mocks to extend)
    - supabase/migrations/20260323000001_create_users.sql (users table schema for firebase_uid column)
  </read_first>
  <action>
**jest.config.js changes:**
1. Update `testMatch` from `['**/__tests__/**/*.test.js']` to `['**/__tests__/**/*.test.{js,ts}']`
2. Update `collectCoverageFrom` from `['src/**/*.{js,jsx}', ...]` to `['src/**/*.{js,jsx,ts,tsx}', '!src/**/*.test.{js,jsx,ts,tsx}', '!**/node_modules/**']`
3. Add `@supabase/supabase-js` to the `transformIgnorePatterns` list so Jest transforms it

**__tests__/setup/jest.setup.js additions (append to existing file, do NOT remove Firebase mocks):**
Add a Supabase client mock block after existing Firebase mocks:

```javascript
// === Supabase Client Mock ===
const mockSupabaseAuth = {
  signInWithOtp: jest.fn(),
  verifyOtp: jest.fn(),
  signOut: jest.fn(),
  getSession: jest.fn(),
  setSession: jest.fn(),
  onAuthStateChange: jest.fn(() => ({ data: { subscription: { unsubscribe: jest.fn() } } })),
  getUser: jest.fn(),
};

const mockSupabaseStorage = {
  from: jest.fn(() => ({
    upload: jest.fn(),
    remove: jest.fn(),
    getPublicUrl: jest.fn((path) => ({ data: { publicUrl: `https://test.supabase.co/storage/v1/object/public/photos/${path}` } })),
    createSignedUrl: jest.fn((path, expiresIn) => Promise.resolve({ data: { signedUrl: `https://test.supabase.co/storage/v1/object/sign/snaps/${path}?token=abc&expires=${expiresIn}` }, error: null })),
    list: jest.fn(),
  })),
};

const mockSupabaseFunctions = {
  invoke: jest.fn(),
};

jest.mock('@supabase/supabase-js', () => ({
  createClient: jest.fn(() => ({
    auth: mockSupabaseAuth,
    storage: mockSupabaseStorage,
    functions: mockSupabaseFunctions,
    from: jest.fn(() => ({
      select: jest.fn().mockReturnThis(),
      insert: jest.fn().mockReturnThis(),
      update: jest.fn().mockReturnThis(),
      delete: jest.fn().mockReturnThis(),
      eq: jest.fn().mockReturnThis(),
      single: jest.fn(),
    })),
  })),
}));

// Export mocks for test access
global.__supabaseMocks = {
  auth: mockSupabaseAuth,
  storage: mockSupabaseStorage,
  functions: mockSupabaseFunctions,
};
```

NOTE: `setSession` is included in the mock -- this is critical for the silent migration flow where the client calls `supabase.auth.setSession({ access_token, refresh_token })` after receiving tokens from the Edge Function.

**SQL migration (20260323000009_add_firebase_uid_and_storage_buckets.sql):**

```sql
-- Add firebase_uid column for migration lookups
ALTER TABLE users ADD COLUMN firebase_uid TEXT UNIQUE;
CREATE INDEX idx_users_firebase_uid ON users (firebase_uid) WHERE firebase_uid IS NOT NULL;

-- Create storage buckets
INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
VALUES
  ('photos', 'photos', true, 52428800, ARRAY['image/webp', 'image/jpeg', 'image/png', 'video/quicktime', 'video/mp4']),
  ('snaps', 'snaps', false, 10485760, ARRAY['image/webp', 'image/jpeg', 'image/png']),
  ('profile-photos', 'profile-photos', true, 5242880, ARRAY['image/webp', 'image/jpeg', 'image/png']),
  ('selects', 'selects', true, 5242880, ARRAY['image/webp', 'image/jpeg', 'image/png']),
  ('comment-images', 'comment-images', true, 10485760, ARRAY['image/webp', 'image/jpeg', 'image/png'])
ON CONFLICT (id) DO NOTHING;

-- Storage RLS policies: public bucket reads
CREATE POLICY "Public read photos" ON storage.objects FOR SELECT USING (bucket_id = 'photos');
CREATE POLICY "Public read profile-photos" ON storage.objects FOR SELECT USING (bucket_id = 'profile-photos');
CREATE POLICY "Public read selects" ON storage.objects FOR SELECT USING (bucket_id = 'selects');
CREATE POLICY "Public read comment-images" ON storage.objects FOR SELECT USING (bucket_id = 'comment-images');

-- Storage RLS policies: authenticated upload to own path
CREATE POLICY "Users upload own photos" ON storage.objects FOR INSERT
  WITH CHECK (bucket_id = 'photos' AND (storage.foldername(name))[1] = auth.uid()::text);
CREATE POLICY "Users upload own profile-photos" ON storage.objects FOR INSERT
  WITH CHECK (bucket_id = 'profile-photos' AND (storage.foldername(name))[1] = auth.uid()::text);
CREATE POLICY "Users upload own selects" ON storage.objects FOR INSERT
  WITH CHECK (bucket_id = 'selects' AND (storage.foldername(name))[1] = auth.uid()::text);
CREATE POLICY "Users upload own comment-images" ON storage.objects FOR INSERT
  WITH CHECK (bucket_id IN ('comment-images') AND auth.role() = 'authenticated');

-- Storage RLS policies: delete own files
CREATE POLICY "Users delete own photos" ON storage.objects FOR DELETE
  USING (bucket_id = 'photos' AND (storage.foldername(name))[1] = auth.uid()::text);
CREATE POLICY "Users delete own profile-photos" ON storage.objects FOR DELETE
  USING (bucket_id = 'profile-photos' AND (storage.foldername(name))[1] = auth.uid()::text);
CREATE POLICY "Users delete own selects" ON storage.objects FOR DELETE
  USING (bucket_id = 'selects' AND (storage.foldername(name))[1] = auth.uid()::text);

-- Snaps: private bucket, upload by sender, read by conversation participant
CREATE POLICY "Snap upload by sender" ON storage.objects FOR INSERT
  WITH CHECK (bucket_id = 'snaps' AND (storage.foldername(name))[1] = auth.uid()::text);
CREATE POLICY "Snap read by participant" ON storage.objects FOR SELECT
  USING (
    bucket_id = 'snaps'
    AND EXISTS (
      SELECT 1 FROM conversations c
      JOIN messages m ON m.conversation_id = c.id
      WHERE m.snap_storage_path = name
        AND (c.participant1_id = auth.uid() OR c.participant2_id = auth.uid())
    )
  );
CREATE POLICY "Snap delete by sender" ON storage.objects FOR DELETE
  USING (bucket_id = 'snaps' AND (storage.foldername(name))[1] = auth.uid()::text);
```
  </action>
  <verify>
    <automated>cd C:/Users/maser/Projects/flick && node -e "const c = require('./jest.config.js'); const ok = c.testMatch[0].includes('{js,ts}') && c.collectCoverageFrom[0].includes('{js,jsx,ts,tsx}'); console.log(ok ? 'PASS' : 'FAIL')" && grep -q "mockSupabaseAuth" __tests__/setup/jest.setup.js && grep -q "setSession" __tests__/setup/jest.setup.js && grep -q "firebase_uid" supabase/migrations/20260323000009_add_firebase_uid_and_storage_buckets.sql && echo "ALL PASS"</automated>
  </verify>
  <done>Jest discovers .test.ts files, Supabase mocks (including setSession) available globally, firebase_uid column and 5 storage buckets with RLS policies defined in migration</done>
</task>

<task type="auto">
  <name>Task 2: Failing test scaffolds (RED phase for TDD)</name>
  <files>__tests__/services/phoneAuthService.test.ts, __tests__/services/signedUrlService.test.ts, __tests__/integration/authMigration.test.ts</files>
  <read_first>
    - __tests__/setup/jest.setup.js (Supabase mock setup from Task 1)
    - src/services/firebase/phoneAuthService.js (understand current API surface for test contracts)
    - src/services/firebase/signedUrlService.js (understand current API surface for test contracts)
    - .planning/phases/13-auth-storage-migration/13-RESEARCH.md (Pattern 1: Supabase Phone OTP, Pattern 2: Silent Auth Migration)
  </read_first>
  <action>
**Write all three test files FIRST, before any implementation. Tests MUST fail (RED state). Commit with message `test(13-01): add failing tests for phone auth, signed URL, and auth migration`.**

**__tests__/services/phoneAuthService.test.ts:**
```typescript
import { sendVerificationCode, verifyCode, getPhoneAuthErrorMessage, validatePhoneNumber, signOut } from '../../src/services/supabase/phoneAuthService';

describe('phoneAuthService', () => {
  beforeEach(() => jest.clearAllMocks());

  describe('validatePhoneNumber', () => {
    it('returns valid with E.164 for valid US number', () => {
      const result = validatePhoneNumber('5551234567', 'US');
      expect(result.valid).toBe(true);
      expect(result.e164).toBe('+15551234567');
    });
    it('returns invalid for bad number', () => {
      const result = validatePhoneNumber('123', 'US');
      expect(result.valid).toBe(false);
      expect(result.error).toBeDefined();
    });
  });

  describe('sendVerificationCode', () => {
    it('calls signInWithOtp with E.164 phone and returns success', async () => {
      global.__supabaseMocks.auth.signInWithOtp.mockResolvedValue({ data: {}, error: null });
      const result = await sendVerificationCode('5551234567', 'US');
      expect(result.success).toBe(true);
      expect(result.e164).toBe('+15551234567');
      expect(global.__supabaseMocks.auth.signInWithOtp).toHaveBeenCalledWith({ phone: '+15551234567' });
    });
    it('returns error for invalid phone', async () => {
      const result = await sendVerificationCode('123', 'US');
      expect(result.success).toBe(false);
      expect(result.error).toBeDefined();
    });
    it('maps Supabase error to user-friendly message', async () => {
      global.__supabaseMocks.auth.signInWithOtp.mockResolvedValue({ data: null, error: { message: 'rate limit exceeded' } });
      const result = await sendVerificationCode('5551234567', 'US');
      expect(result.success).toBe(false);
      expect(result.error).toContain('Too many attempts');
    });
  });

  describe('verifyCode', () => {
    it('calls verifyOtp with phone, token, and type sms', async () => {
      const mockSession = { access_token: 'abc', refresh_token: 'def' };
      global.__supabaseMocks.auth.verifyOtp.mockResolvedValue({ data: { session: mockSession }, error: null });
      const result = await verifyCode('+15551234567', '123456');
      expect(result.success).toBe(true);
      expect(result.session).toEqual(mockSession);
      expect(global.__supabaseMocks.auth.verifyOtp).toHaveBeenCalledWith({ phone: '+15551234567', token: '123456', type: 'sms' });
    });
    it('returns error for invalid code', async () => {
      global.__supabaseMocks.auth.verifyOtp.mockResolvedValue({ data: null, error: { message: 'otp_not_found' } });
      const result = await verifyCode('+15551234567', '000000');
      expect(result.success).toBe(false);
      expect(result.error).toContain('Invalid code');
    });
  });

  describe('getPhoneAuthErrorMessage', () => {
    it('maps otp_expired', () => {
      expect(getPhoneAuthErrorMessage('otp_expired')).toContain('expired');
    });
    it('maps otp_not_found', () => {
      expect(getPhoneAuthErrorMessage('otp_not_found')).toContain('Invalid code');
    });
  });

  describe('signOut', () => {
    it('calls supabase.auth.signOut', async () => {
      global.__supabaseMocks.auth.signOut.mockResolvedValue({ error: null });
      const result = await signOut();
      expect(result.success).toBe(true);
    });
  });
});
```

**__tests__/services/signedUrlService.test.ts:**
```typescript
import { getPhotoUrl, getSnapUrl } from '../../src/services/supabase/signedUrlService';

describe('signedUrlService', () => {
  beforeEach(() => jest.clearAllMocks());

  describe('getPhotoUrl', () => {
    it('returns public CDN URL synchronously', () => {
      const url = getPhotoUrl('photos/uid/photo123.webp');
      expect(url).toContain('public');
      expect(url).toContain('photo123.webp');
    });
  });

  describe('getSnapUrl', () => {
    it('returns signed URL with 300s expiry', async () => {
      const url = await getSnapUrl('uid/snap123.webp');
      expect(url).toContain('snap123.webp');
      expect(global.__supabaseMocks.storage.from).toHaveBeenCalledWith('snaps');
    });
    it('returns null on error', async () => {
      global.__supabaseMocks.storage.from.mockReturnValueOnce({
        createSignedUrl: jest.fn().mockResolvedValue({ data: null, error: { message: 'not found' } }),
      });
      const url = await getSnapUrl('uid/missing.webp');
      expect(url).toBeNull();
    });
  });
});
```

**__tests__/integration/authMigration.test.ts:**

This test validates the client-side silent migration flow. It mocks the Firebase auth module and Supabase functions.invoke to simulate the Edge Function response.

```typescript
// Integration test for silent Firebase-to-Supabase auth migration (AUTH-02)
// Tests the client-side migration bridge in AuthContext

describe('Silent Auth Migration', () => {
  beforeEach(() => jest.clearAllMocks());

  it('detects Firebase token and calls migrate-firebase-auth Edge Function', async () => {
    // Mock Firebase auth to return a current user with token
    // Mock supabase.functions.invoke to return success with tokens
    // Verify: supabase.functions.invoke called with { firebaseToken }
    // Verify: supabase.auth.setSession called with { access_token, refresh_token }
    const mockInvoke = global.__supabaseMocks.functions.invoke;
    mockInvoke.mockResolvedValue({
      data: {
        success: true,
        migrated: true,
        access_token: 'test-access-token',
        refresh_token: 'test-refresh-token',
        supabaseUserId: 'supa-uuid-123',
      },
      error: null,
    });

    // This test will fail until AuthContext implements the migration bridge
    // that calls setSession with the returned tokens
    expect(mockInvoke).not.toHaveBeenCalled(); // Placeholder -- will be replaced with actual AuthContext test
    // TODO: Import and test the migration function from AuthContext once implemented
  });

  it('Edge Function returns access_token and refresh_token (not just supabaseUserId)', async () => {
    const mockInvoke = global.__supabaseMocks.functions.invoke;
    mockInvoke.mockResolvedValue({
      data: {
        success: true,
        migrated: true,
        access_token: 'real-access-token',
        refresh_token: 'real-refresh-token',
        supabaseUserId: 'supa-uuid-123',
      },
      error: null,
    });

    // Verify response shape includes tokens (not just userId)
    const response = await mockInvoke('migrate-firebase-auth', { body: { firebaseToken: 'fb-token' } });
    expect(response.data.access_token).toBeDefined();
    expect(response.data.refresh_token).toBeDefined();
    expect(response.data.success).toBe(true);
  });

  it('falls back to re-verification when migration fails', async () => {
    const mockInvoke = global.__supabaseMocks.functions.invoke;
    mockInvoke.mockResolvedValue({
      data: null,
      error: { message: 'Firebase token expired' },
    });

    const response = await mockInvoke('migrate-firebase-auth', { body: { firebaseToken: 'expired-token' } });
    expect(response.error).toBeDefined();
    // Client should navigate to PhoneInputScreen for re-verification
  });
});
```

**After writing all three test files, run them to confirm RED state:**
```bash
npx jest --testPathPattern="(phoneAuthService|signedUrlService|authMigration)" --no-coverage 2>&1 | tail -20
```
Tests should fail because the service files do not exist yet. Commit the failing tests.
  </action>
  <verify>
    <automated>cd C:/Users/maser/Projects/flick && test -f __tests__/services/phoneAuthService.test.ts && test -f __tests__/services/signedUrlService.test.ts && test -f __tests__/integration/authMigration.test.ts && echo "PASS"</automated>
  </verify>
  <done>All three test files exist in RED state. phoneAuthService tests cover send/verify/signOut/validate/errorMapping. signedUrlService tests cover public URL and signed URL. authMigration tests validate Edge Function response shape includes access_token + refresh_token and client calls setSession.</done>
</task>

<task type="auto">
  <name>Task 3: Implement phoneAuthService.ts + signedUrlService.ts (GREEN phase)</name>
  <files>src/services/supabase/phoneAuthService.ts, src/services/supabase/signedUrlService.ts</files>
  <read_first>
    - __tests__/services/phoneAuthService.test.ts (tests to make pass -- from Task 2)
    - __tests__/services/signedUrlService.test.ts (tests to make pass -- from Task 2)
    - src/services/firebase/phoneAuthService.js (current implementation to match API surface)
    - src/services/firebase/signedUrlService.js (current implementation to match API surface)
    - src/lib/supabase.ts (client import path)
    - .planning/phases/13-auth-storage-migration/13-RESEARCH.md (Pattern 1: Supabase Phone OTP)
  </read_first>
  <action>
**Implement both services to make tests from Task 2 pass (GREEN state). Commit with `feat(13-01): implement phone auth and signed URL services`.**

**src/services/supabase/phoneAuthService.ts:**

Create a new file with these exports, using the Supabase client from `src/lib/supabase.ts`:

1. `getPhoneAuthErrorMessage(errorMessage: string): string` -- Maps Supabase auth error messages to user-friendly strings. Key mappings:
   - `'otp_expired'` or contains `'expired'` -> `'Code expired. Tap Resend to get a new code.'`
   - `'otp_not_found'` or contains `'invalid'` or `'not found'` -> `'Invalid code. Please check and try again.'`
   - Contains `'rate limit'` or `'too many'` -> `'Too many attempts. Please wait a moment and try again.'`
   - Contains `'phone'` and `'invalid'` -> `'Invalid phone number. Please check the number and try again.'`
   - Default: `'Unable to send verification code. Please try again.'`

2. `validatePhoneNumber(phoneNumber: string, countryCode: string): { valid: boolean; e164?: string; error?: string }` -- Uses `parsePhoneNumberFromString` from `libphonenumber-js` to validate and format. Returns `{ valid: true, e164: formatted }` or `{ valid: false, error: 'Please enter a valid phone number' }`.

3. `sendVerificationCode(phoneNumber: string, countryCode: string): Promise<{ success: boolean; e164?: string; error?: string }>` -- Validates phone, calls `supabase.auth.signInWithOtp({ phone: e164 })`. Returns `{ success: true, e164 }` on success or `{ success: false, error: mappedMessage }` on failure. NOTE: No ConfirmationResult stored -- Supabase OTP is stateless.

4. `verifyCode(phone: string, code: string): Promise<{ success: boolean; session?: any; error?: string }>` -- Calls `supabase.auth.verifyOtp({ phone, token: code, type: 'sms' })`. Returns `{ success: true, session: data.session }` or `{ success: false, error: mappedMessage }`.

5. `signOut(): Promise<{ success: boolean; error?: string }>` -- Calls `supabase.auth.signOut()`.

All functions use `logger` from `../../utils/logger` for debug/error logging (same pattern as Firebase version). All async functions wrap in try/catch returning `{ success: false, error }`.

**src/services/supabase/signedUrlService.ts:**

1. `getPhotoUrl(storagePath: string, bucket?: string): string` -- SYNCHRONOUS. Calls `supabase.storage.from(bucket || 'photos').getPublicUrl(storagePath)` and returns `data.publicUrl`. For profile photos, selects, and comment images, pass bucket name as second param.

2. `getSnapUrl(storagePath: string): Promise<string | null>` -- ASYNC. Calls `supabase.storage.from('snaps').createSignedUrl(storagePath, 300)`. Returns `data.signedUrl` or `null` on error.

Both import `supabase` from the lib path established in Phase 12. Check if `@/lib/supabase` or relative path convention is used.
  </action>
  <verify>
    <automated>cd C:/Users/maser/Projects/flick && npx jest --testPathPattern="services/(phoneAuthService|signedUrlService)" --no-coverage -x 2>&1 | tail -20</automated>
  </verify>
  <done>All phoneAuthService and signedUrlService tests pass (GREEN state). Supabase phone auth service handles send/verify/signOut with stateless OTP pattern. Signed URL service returns public CDN URLs for photos and 5-minute signed URLs for snaps.</done>
</task>

</tasks>

<verification>
- `npx jest --testPathPattern="services/(phoneAuthService|signedUrlService)" --no-coverage -x` passes
- `npx jest --testPathPattern="integration/authMigration" --no-coverage` runs (may still have placeholder tests)
- `grep -r "signInWithOtp" src/services/supabase/phoneAuthService.ts` returns match
- `grep -r "getPublicUrl" src/services/supabase/signedUrlService.ts` returns match
- `grep -r "createSignedUrl" src/services/supabase/signedUrlService.ts` returns match
- Migration file exists at `supabase/migrations/20260323000009_add_firebase_uid_and_storage_buckets.sql`
</verification>

<success_criteria>
- Phone auth service provides stateless OTP flow (no ConfirmationResult pattern)
- Signed URL service provides instant public URLs for photos and async signed URLs for snaps
- Jest can run .test.ts files with Supabase mocks (including setSession mock)
- Database migration adds firebase_uid column and configures all 5 storage buckets with RLS
- Auth migration integration test exists (RED state for Plan 02 to make GREEN)
- TDD commits: RED (tests only) then GREEN (implementation) as separate commits
</success_criteria>

<output>
After completion, create `.planning/phases/13-auth-storage-migration/13-01-SUMMARY.md`
</output>
