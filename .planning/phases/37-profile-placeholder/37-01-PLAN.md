---
phase: 37-profile-placeholder
plan: 01
type: execute
---

<objective>
Simplify ProfileScreen to a minimal placeholder with just Friends button and Logout button.

Purpose: Create a bare-bones Profile tab until the full Profile redesign in a future milestone.
Output: Streamlined ProfileScreen.js with two buttons only.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-profile-placeholder/37-CONTEXT.md

# Key files:

@src/screens/ProfileScreen.js
@src/navigation/AppNavigator.js (for FriendsList route reference)

**From CONTEXT.md:**

- Only Friends button and Logout button needed
- Remove: profile editing, stats display, settings, any polish
- This is intentionally bare-bones - real Profile work comes later
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Strip ProfileScreen to minimal placeholder</name>
  <files>src/screens/ProfileScreen.js</files>
  <action>
Replace current ProfileScreen with a minimal version containing only:

1. **SafeAreaView container** - Dark background matching app theme
2. **Header** - Simple "Profile" title (no settings button)
3. **Two buttons centered vertically:**
   - "Friends" button - navigates to FriendsList screen
   - "Sign Out" button - calls signOut() from AuthContext

Remove ALL of the following:

- Stats loading useEffect and state
- Profile photo section
- Username/bio display
- Stats display (posts, friends, reactions)
- Coming Soon section
- Edit Profile button
- Settings button in header
- Firestore imports and db initialization
- getFriendUserIds import
- stats state variable

Keep:

- useAuth hook (for signOut and user)
- useNavigation hook (for Friends navigation)
- SafeAreaView from react-native-safe-area-context
- colors import
- logger import (for button press logging)

Layout: Center the two buttons in the screen with generous spacing. Use Button component for consistent styling. Friends button should be primary/outline variant, Sign Out should be secondary variant.
</action>
<verify>
Run `npx expo start` and navigate to Profile tab:

- Screen shows "Profile" header
- Two buttons visible: "Friends" and "Sign Out"
- Friends button navigates to FriendsList screen
- Sign Out button logs user out
- No stats, no profile photo, no coming soon section
  </verify>
  <done>
- ProfileScreen displays only header, Friends button, and Sign Out button
- Friends button navigates to FriendsList
- Sign Out button triggers signOut()
- All removed code: stats, profile section, coming soon, edit profile, settings
- No TypeScript/lint errors
  </done>
  </task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] ProfileScreen shows only "Profile" title and two buttons
- [ ] Friends button navigates to FriendsList screen correctly
- [ ] Sign Out button logs user out correctly
- [ ] No console errors or warnings
- [ ] App builds without errors
</verification>

<success_criteria>

- ProfileScreen reduced to minimal placeholder
- Only Friends and Sign Out functionality remain
- All unnecessary code removed
- Navigation to FriendsList works
- Sign out functionality works
- Phase 37 complete
  </success_criteria>

<output>
After completion, create `.planning/phases/37-profile-placeholder/37-01-SUMMARY.md`
</output>
