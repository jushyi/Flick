---
phase: 19-performance-polish
plan: 04
type: execute
wave: 3
depends_on: [19-01, 19-02, 19-03]
files_modified:
  - src/screens/FeedScreen.js
  - src/screens/MessagesListScreen.js
  - src/screens/FriendsScreen.js
  - src/screens/NotificationsScreen.js
  - src/screens/DarkroomScreen.js
  - src/screens/ProfileScreen.js
  - src/screens/ActivityScreen.js
  - src/components/CommentsBottomSheet.js
  - src/components/AlbumGrid.js
  - src/hooks/useFeedPhotos.ts
  - src/hooks/usePhotoDetailModal.js
  - src/components/FriendStoryCard.js
  - App.js
autonomous: false
requirements: [PERF-03, PERF-07, PERF-10, PERF-11]

must_haves:
  truths:
    - "All 9 screens show skeleton screens during initial load (no cache) instead of blank/spinner"
    - "All list views show EmptyState component when data is empty"
    - "Feed card images load at 400px via transform URL, PhotoDetail loads full-res"
    - "Feed prefetches first image of each friend's story at 400px on load"
    - "Story viewing prefetches next 3 photos at full-res"
    - "Toast component renders at App.js root level"
  artifacts:
    - path: "src/screens/FeedScreen.js"
      provides: "FeedSkeleton + EmptyState + transform URLs integrated"
    - path: "App.js"
      provides: "Toast component at root level"
  key_links:
    - from: "src/screens/FeedScreen.js"
      to: "src/components/skeletons/FeedSkeleton.tsx"
      via: "ListEmptyComponent or conditional render"
      pattern: "FeedSkeleton"
    - from: "src/hooks/useFeedPhotos.ts"
      to: "src/utils/imageUrl.ts"
      via: "import { getTransformedPhotoUrl, FEED_CARD_WIDTH }"
      pattern: "getTransformedPhotoUrl"
    - from: "App.js"
      to: "src/components/Toast.tsx"
      via: "import and render after NavigationContainer"
      pattern: "Toast"
---

<objective>
Wire skeleton screens, empty states, image transforms, and prefetching into all screens. Place Toast at app root.

Purpose: This plan connects all the foundation components (Plans 01-02) into the actual screens. Skeleton screens replace blank/spinner states on 9 screens (per D-03). EmptyState replaces inline empty renders (per D-13). Transform URLs serve feed images at 400px (per D-08). Prefetching loads story images ahead of viewing (per D-09). Toast at root enables error notifications (per D-05). Depends on Plan 03 because Task 2 modifies useFeedPhotos.ts which Plan 03 also touches for persist:true.

Output: 13 modified files covering all screen integrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-performance-polish/19-CONTEXT.md
@.planning/phases/19-performance-polish/19-RESEARCH.md
@.planning/phases/19-performance-polish/19-UI-SPEC.md
@.planning/phases/19-performance-polish/19-01-SUMMARY.md
@.planning/phases/19-performance-polish/19-02-SUMMARY.md
@.planning/phases/19-performance-polish/19-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From src/components/skeletons/SkeletonBase.tsx:
```typescript
export function useShimmer(): Animated.Value;
export function SkeletonShape(props: { width: number | string; height: number; borderRadius?: number; shimmerPosition: Animated.Value; style?: ViewStyle }): JSX.Element;
```

From src/components/skeletons/ (all skeleton exports):
```typescript
export function FeedSkeleton(): JSX.Element;
export function ConversationsSkeleton(): JSX.Element;
export function FriendsSkeleton(): JSX.Element;
export function CommentsSkeleton(): JSX.Element;
export function NotificationsSkeleton(): JSX.Element;
export function AlbumsSkeleton(): JSX.Element;
export function DarkroomSkeleton(): JSX.Element;
export function ProfilePhotoGridSkeleton(): JSX.Element;
export function ActivitySkeleton(): JSX.Element;
```

From src/components/Toast.tsx:
```typescript
export { toastConfig };
// Renders <Toast config={toastConfig} position="bottom" bottomOffset={100} visibilityTime={2000} />
```

<!-- From Plan 02 -->
From src/utils/imageUrl.ts:
```typescript
export const FEED_CARD_WIDTH = 400;
export function getTransformedPhotoUrl(storagePath: string, options?: { width?: number; quality?: number; bucket?: string }): string;
export function isUrlNearExpiry(signedUrl: string, thresholdMs?: number): boolean;
```

From src/components/EmptyState.tsx:
```typescript
export function EmptyState(props: { icon: string; message: string; ctaLabel?: string; onCtaPress?: () => void }): JSX.Element;
```

<!-- Empty State Copywriting from UI-SPEC -->
| Screen | Icon | Message | CTA Label | CTA Action |
|--------|------|---------|-----------|------------|
| FeedScreen | camera-outline | "No photos yet" | "Add friends" | Navigate to FriendsScreen |
| MessagesScreen | chatbubble-outline | "No conversations yet" | "Start a chat" | Navigate to NewMessage |
| FriendsScreen | people-outline | "No friends yet" | "Find friends" | Navigate to ContactsSync |
| CommentsBottomSheet | chatbubble-outline | "No comments yet" | "Be the first" | Focus comment input |
| NotificationsScreen | notifications-outline | "No notifications yet" | (none) | -- |
| AlbumGridScreen | images-outline | "No photos in this album" | (none) | -- |
| DarkroomScreen | tab-darkroom | "Nothing developing" | (none) | -- |
| ProfileScreen (grid) | image-outline | "No photos yet" | "Take a photo" | Navigate to Camera |
| ActivityScreen | notifications-outline | "No activity yet" | (none) | -- |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire skeletons + empty states into 9 screens + Toast in App.js</name>
  <files>
    src/screens/FeedScreen.js,
    src/screens/MessagesListScreen.js,
    src/screens/FriendsScreen.js,
    src/screens/NotificationsScreen.js,
    src/screens/DarkroomScreen.js,
    src/screens/ProfileScreen.js,
    src/screens/ActivityScreen.js,
    src/components/CommentsBottomSheet.js,
    src/components/AlbumGrid.js,
    App.js
  </files>
  <read_first>
    src/screens/FeedScreen.js,
    src/screens/MessagesListScreen.js,
    src/screens/FriendsScreen.js,
    src/screens/NotificationsScreen.js,
    src/screens/DarkroomScreen.js,
    src/screens/ProfileScreen.js,
    src/screens/ActivityScreen.js,
    src/components/CommentsBottomSheet.js,
    src/components/AlbumGrid.js,
    App.js,
    src/components/skeletons/FeedSkeleton.tsx,
    src/components/EmptyState.tsx,
    src/components/Toast.tsx
  </read_first>
  <action>
    **For each of the 9 screens, apply the skeleton/empty state pattern from 19-UI-SPEC Interaction Contracts:**

    ```
    isLoading && !data  -> Show skeleton component
    isLoading && data   -> Show real data (stale-while-revalidate)
    !isLoading && empty -> Show EmptyState
    !isLoading && data  -> Show real content
    ```

    **Screen-by-screen instructions:**

    1. **FeedScreen.js**: Import FeedSkeleton and EmptyState. In the render logic:
       - If `isLoading && !feedData`, render `<FeedSkeleton />`
       - Replace existing empty state (TakeFirstPhotoCard or sad emoji) with `<EmptyState icon="camera-outline" message="No photos yet" ctaLabel="Add friends" onCtaPress={() => navigation.navigate('FriendsList')} />`
       - Use FlatList `ListEmptyComponent` if the screen uses FlatList, otherwise use conditional rendering

    2. **MessagesListScreen.js**: Import ConversationsSkeleton and EmptyState.
       - If loading and no cached data, render `<ConversationsSkeleton />`
       - Replace existing renderEmptyState with `<EmptyState icon="chatbubble-outline" message="No conversations yet" ctaLabel="Start a chat" onCtaPress={() => navigation.navigate('NewMessage')} />`

    3. **FriendsScreen.js**: Import FriendsSkeleton and EmptyState.
       - If loading and no cached data, render `<FriendsSkeleton />`
       - Replace existing empty state with `<EmptyState icon="people-outline" message="No friends yet" ctaLabel="Find friends" onCtaPress={() => navigation.navigate('ContactsSync')} />`

    4. **NotificationsScreen.js**: Import NotificationsSkeleton and EmptyState.
       - If loading and no cached data, render `<NotificationsSkeleton />`
       - Replace existing empty state with `<EmptyState icon="notifications-outline" message="No notifications yet" />`

    5. **DarkroomScreen.js**: Import DarkroomSkeleton and EmptyState.
       - If loading and no cached data, render `<DarkroomSkeleton />`
       - Replace existing empty state with `<EmptyState icon="tab-darkroom" message="Nothing developing" />`

    6. **ProfileScreen.js** (photo grid section): Import ProfilePhotoGridSkeleton and EmptyState.
       - If loading photos and no cached data, render `<ProfilePhotoGridSkeleton />`
       - Replace existing empty state with `<EmptyState icon="image-outline" message="No photos yet" ctaLabel="Take a photo" onCtaPress={() => navigation.navigate('MainTabs', { screen: 'Camera' })} />`

    7. **ActivityScreen.js**: Import ActivitySkeleton and EmptyState.
       - If loading and no cached data, render `<ActivitySkeleton />`
       - Replace existing empty state with `<EmptyState icon="notifications-outline" message="No activity yet" />`

    8. **CommentsBottomSheet.js**: Import CommentsSkeleton and EmptyState.
       - If loading comments and no cached data, render `<CommentsSkeleton />`
       - Replace existing renderEmpty with `<EmptyState icon="chatbubble-outline" message="No comments yet" ctaLabel="Be the first" onCtaPress={() => { /* focus comment input ref */ }} />`

    9. **AlbumGrid.js** (or AlbumGridScreen): Import AlbumsSkeleton and EmptyState.
       - If loading and no cached data, render `<AlbumsSkeleton />`
       - Add `<EmptyState icon="images-outline" message="No photos in this album" />`

    **Toast in App.js:**
    - Import the Toast component from `src/components/Toast.tsx`
    - Place `<Toast />` (or the exported component) as the LAST child in the App.js render, AFTER NavigationContainer (per RESEARCH.md Pitfall 6)
    - This ensures toasts render above all navigation screens and modals

    **Important notes:**
    - Read each screen file FIRST to understand its current loading/empty state pattern before modifying
    - Preserve all existing functionality -- only replace loading and empty state renders
    - Follow import order from CLAUDE.md: React, RN core, third-party, services, components, hooks, utils
    - Do NOT change data fetching logic, only the render conditionals
  </action>
  <verify>
    <automated>npm test -- --bail</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/FeedScreen.js contains "FeedSkeleton" and "EmptyState"
    - src/screens/MessagesListScreen.js contains "ConversationsSkeleton" and "EmptyState"
    - src/screens/FriendsScreen.js contains "FriendsSkeleton" and "EmptyState"
    - src/screens/NotificationsScreen.js contains "NotificationsSkeleton" and "EmptyState"
    - src/screens/DarkroomScreen.js contains "DarkroomSkeleton" and "EmptyState"
    - src/screens/ProfileScreen.js contains "ProfilePhotoGridSkeleton" and "EmptyState"
    - src/screens/ActivityScreen.js contains "ActivitySkeleton" and "EmptyState"
    - src/components/CommentsBottomSheet.js contains "CommentsSkeleton" and "EmptyState"
    - App.js contains "Toast" import from components/Toast
    - All 9 screens import EmptyState from '../components/EmptyState' or '@/components/EmptyState'
  </acceptance_criteria>
  <done>All 9 screens have skeleton loading states and consistent empty states. Toast is mounted at app root.</done>
</task>

<task type="auto">
  <name>Task 2: Wire image transform URLs + prefetching into feed and story hooks</name>
  <files>
    src/hooks/useFeedPhotos.ts,
    src/hooks/usePhotoDetailModal.js,
    src/components/FriendStoryCard.js
  </files>
  <read_first>
    src/hooks/useFeedPhotos.ts,
    src/hooks/usePhotoDetailModal.js,
    src/components/FriendStoryCard.js,
    src/utils/imageUrl.ts
  </read_first>
  <action>
    **IMPORTANT:** Re-read useFeedPhotos.ts fresh. Plan 03 may have added `meta: { persist: true }` to this file. Preserve all Plan 03 changes when making edits here.

    **Image transform URLs (per D-08, PERF-06):**

    1. **useFeedPhotos.ts**: In the feed data processing (where photo URLs are mapped for display), transform public photo URLs to use 400px width:
       - Import `{ getTransformedPhotoUrl, FEED_CARD_WIDTH }` from '@/utils/imageUrl'
       - Where photo URLs are prepared for feed cards, replace the raw `photo_url` or `photoUrl` with `getTransformedPhotoUrl(storagePath, { width: FEED_CARD_WIDTH })`
       - NOTE: The feed data comes from a Supabase RPC. The URL transformation should happen in the hook when processing the response data, NOT in the RPC. Read the hook to understand where photo URLs are currently being used and add the transform there.
       - If the hook returns photo objects with a `photoUrl` field, map it through getTransformedPhotoUrl
       - Keep the original full URL available as `fullPhotoUrl` for when PhotoDetail needs it

    2. **FriendStoryCard.js**: When rendering the story card thumbnail image:
       - Import `{ getTransformedPhotoUrl, FEED_CARD_WIDTH }` from '@/utils/imageUrl'
       - Use transformed 400px URL for the card image `source` prop
       - This ensures story thumbnails in the feed load smaller images

    **Prefetching (per D-09, PERF-10, PERF-11):**

    3. **useFeedPhotos.ts**: Add prefetch logic after feed data loads:
       - Import `{ Image }` from 'expo-image'
       - After feed data is successfully fetched (in a useEffect watching the data), extract the first photo URL from each friend's story group
       - Transform each to 400px: `getTransformedPhotoUrl(storagePath, { width: FEED_CARD_WIDTH })`
       - Call `Image.prefetch(urls, 'memory-disk')` wrapped in try/catch with logger.warn on failure (matching existing prefetch pattern from useDarkroom)
       - Throttle: only prefetch if urls.length > 0 and only on initial data load (not on every revalidation). Use a ref to track if initial prefetch has been done.

    4. **usePhotoDetailModal.js**: Enhance existing prefetch for story viewing:
       - Import `{ getTransformedPhotoUrl }` from '@/utils/imageUrl'
       - When story viewing starts (or navigates to next photo), prefetch the next 3 photos at FULL resolution (no width transform -- use `getTransformedPhotoUrl(storagePath)` which returns untransformed URL)
       - When transitioning between friends in stories mode, prefetch first 3 photos of next friend at full-res
       - Use existing `Image.prefetch(urls, 'memory-disk')` pattern
       - Wrap in try/catch with logger.warn (existing pattern)
  </action>
  <verify>
    <automated>npm test -- __tests__/hooks/useFeedPhotos.test.ts --bail</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useFeedPhotos.ts contains "getTransformedPhotoUrl" import
    - src/hooks/useFeedPhotos.ts contains "FEED_CARD_WIDTH" import
    - src/hooks/useFeedPhotos.ts contains "Image.prefetch" call
    - src/hooks/useFeedPhotos.ts still contains "persist: true" (preserved from Plan 03)
    - src/hooks/usePhotoDetailModal.js contains "getTransformedPhotoUrl" import
    - src/components/FriendStoryCard.js contains "getTransformedPhotoUrl" import
    - src/hooks/useFeedPhotos.ts contains "memory-disk" (prefetch cache policy)
  </acceptance_criteria>
  <done>Feed images served at 400px. Prefetching loads first image per friend on feed load and next 3 during story viewing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify skeleton screens, empty states, and image loading</name>
  <files>
    src/screens/FeedScreen.js,
    src/screens/MessagesListScreen.js,
    App.js
  </files>
  <action>
    Human verification of the complete performance polish integration. All automated tasks are complete --
    this checkpoint confirms visual quality and real-device behavior that cannot be verified by unit tests.
    Present the verification checklist to the user and wait for approval.
  </action>
  <verify>
    <automated>npm test -- --bail</automated>
  </verify>
  <done>User has visually confirmed skeleton screens, empty states, image transforms, and prefetch behavior on a real device or simulator.</done>
</task>

</tasks>

<verification>
- `npm test -- --bail` passes (full suite)
- All 9 screens have skeleton + empty state integration
- Feed images use ?width=400 transform
- Prefetch calls present in useFeedPhotos and usePhotoDetailModal
- Toast mounted in App.js after NavigationContainer
- useFeedPhotos.ts preserves persist: true from Plan 03
</verification>

<success_criteria>
- Skeleton screens show on all 9 screens during initial load (per D-02, D-03, PERF-03)
- EmptyState component used consistently on all list views (per D-11, D-12, D-13, PERF-07)
- Feed card images served at 400px, PhotoDetail at full-res (per D-08, PERF-06)
- First image per friend prefetched on feed load, next 3 during story viewing (per D-09, PERF-10, PERF-11)
- Human verification confirms visual quality and performance improvement
</success_criteria>

<output>
After completion, create `.planning/phases/19-performance-polish/19-04-SUMMARY.md`
</output>
