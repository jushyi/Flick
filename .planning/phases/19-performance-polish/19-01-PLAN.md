---
phase: 19-performance-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/skeletons/SkeletonBase.tsx
  - src/components/skeletons/FeedSkeleton.tsx
  - src/components/skeletons/ConversationsSkeleton.tsx
  - src/components/skeletons/FriendsSkeleton.tsx
  - src/components/skeletons/CommentsSkeleton.tsx
  - src/components/skeletons/NotificationsSkeleton.tsx
  - src/components/skeletons/AlbumsSkeleton.tsx
  - src/components/skeletons/DarkroomSkeleton.tsx
  - src/components/skeletons/ProfilePhotoGridSkeleton.tsx
  - src/components/skeletons/ActivitySkeleton.tsx
  - src/components/Toast.tsx
  - __tests__/components/skeletons/SkeletonBase.test.tsx
autonomous: true
requirements: [PERF-03]

must_haves:
  truths:
    - "All 9 skeleton screens render matching shimmer animations without errors"
    - "SkeletonBase provides reusable useShimmer hook and SkeletonShape primitive"
    - "Toast config renders pixel-art styled error toasts"
  artifacts:
    - path: "src/components/skeletons/SkeletonBase.tsx"
      provides: "Shared shimmer hook + SkeletonShape primitive"
      exports: ["useShimmer", "SkeletonShape"]
    - path: "src/components/skeletons/FeedSkeleton.tsx"
      provides: "Feed skeleton matching FeedLoadingSkeleton layout"
      exports: ["FeedSkeleton"]
    - path: "src/components/skeletons/ConversationsSkeleton.tsx"
      provides: "Conversations list skeleton"
      exports: ["ConversationsSkeleton"]
    - path: "src/components/Toast.tsx"
      provides: "Custom toast config for pixel art styling"
      exports: ["toastConfig"]
  key_links:
    - from: "src/components/skeletons/FeedSkeleton.tsx"
      to: "src/components/skeletons/SkeletonBase.tsx"
      via: "import { useShimmer, SkeletonShape }"
      pattern: "import.*SkeletonBase"
---

<objective>
Create all skeleton screen components and toast configuration for Phase 19 performance polish.

Purpose: Skeleton screens replace blank/spinner loading states on 9 screens (per D-03). The SkeletonBase extracts the shimmer pattern from the existing FeedLoadingSkeleton into a reusable primitive. Toast config enables pixel-art styled error notifications for optimistic update failures (per D-05). react-native-toast-message is installed as the only new dependency.

Output: 10 skeleton files (1 base + 9 screens), 1 toast config, 1 test file. Plus react-native-toast-message installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-performance-polish/19-CONTEXT.md
@.planning/phases/19-performance-polish/19-RESEARCH.md
@.planning/phases/19-performance-polish/19-UI-SPEC.md

@src/components/FeedLoadingSkeleton.js
@src/constants/colors.js
@src/constants/spacing.js
@src/constants/layout.js
@src/constants/typography.js
@src/lib/queryKeys.ts

<interfaces>
<!-- Existing design constants the executor needs -->

From src/constants/colors.js:
```javascript
export const colors = {
  background: {
    primary: '#0A0A1A',
    secondary: '#161628',
    tertiary: '#252540',
  },
  text: {
    primary: '#E0E0F0',
    secondary: '#7B7B9E',
    tertiary: '#4D4D6A',
  },
  status: {
    danger: '#FF3333',
  },
};
```

From src/constants/spacing.js:
```javascript
// 8px base grid: xxs=4, xs=8, sm=12, md=16, lg=24, xl=32, xxl=40, xxxl=48
```

From src/constants/layout.js:
```javascript
export const layout = {
  borderRadius: { xs: 0, sm: 2, md: 4, lg: 4, xl: 6, round: 9999 },
  dimensions: { tabBarHeight: Platform.OS === 'ios' ? 65 : 54, avatarSmall: 32, avatarMedium: 40 },
};
```

From src/components/FeedLoadingSkeleton.js:
```javascript
// Key constants to match:
const SHIMMER_WIDTH = 100;
const STORY_PHOTO_WIDTH = 88;
const STORY_PHOTO_HEIGHT = 130;
const STORY_BORDER_WIDTH = 3;
const STORY_PROFILE_SIZE = 32;
const FEED_PROFILE_SIZE = 36;
// Shimmer: Animated.timing, duration: 800, useNativeDriver: true
// Shape fill: colors.background.tertiary
// Shimmer color: rgba(255, 255, 255, 0.1)
// All shapes have overflow: 'hidden'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install toast-message, create SkeletonBase + first 5 skeleton screens + tests</name>
  <files>
    package.json,
    src/components/skeletons/SkeletonBase.tsx,
    src/components/skeletons/FeedSkeleton.tsx,
    src/components/skeletons/ConversationsSkeleton.tsx,
    src/components/skeletons/FriendsSkeleton.tsx,
    src/components/skeletons/CommentsSkeleton.tsx,
    src/components/skeletons/NotificationsSkeleton.tsx,
    __tests__/components/skeletons/SkeletonBase.test.tsx
  </files>
  <read_first>
    src/components/FeedLoadingSkeleton.js,
    src/constants/colors.js,
    src/constants/spacing.js,
    src/constants/layout.js,
    src/constants/typography.js,
    src/screens/MessagesListScreen.js,
    src/screens/FriendsScreen.js,
    src/screens/NotificationsScreen.js,
    src/components/CommentInput.js
  </read_first>
  <behavior>
    - Test: SkeletonShape renders a View with backgroundColor colors.background.tertiary and overflow hidden
    - Test: useShimmer returns an Animated.Value
    - Test: FeedSkeleton renders without crashing and matches FeedLoadingSkeleton layout (4 story cards, 2 feed cards)
    - Test: ConversationsSkeleton renders 6 message row placeholders
    - Test: FriendsSkeleton renders friend row placeholders
    - Test: CommentsSkeleton renders comment row placeholders
    - Test: NotificationsSkeleton renders notification row placeholders
  </behavior>
  <action>
    1. Run `npm install react-native-toast-message@2.3.3` to install the only new dependency (per RESEARCH.md)

    2. Create `src/components/skeletons/SkeletonBase.tsx`:
       - Export `useShimmer()` hook: creates `Animated.Value(-100)`, runs `Animated.loop(Animated.timing({ toValue: SCREEN_WIDTH, duration: 800, useNativeDriver: true }))`, returns the animated value
       - Export `SkeletonShape` component with props: `{ width: number | string; height: number; borderRadius?: number; shimmerPosition: Animated.Value; style?: ViewStyle }`
       - SkeletonShape renders a View with `backgroundColor: colors.background.tertiary`, `overflow: 'hidden'`, and an inner `Animated.View` shimmer bar with `backgroundColor: 'rgba(255, 255, 255, 0.1)'`, `position: 'absolute'`, `width: 100`, `transform: [{ translateX: shimmerPosition }]`
       - Export `SHIMMER_WIDTH = 100` constant
       - Use `Dimensions.get('window').width` for SCREEN_WIDTH (same as FeedLoadingSkeleton)

    3. Create `src/components/skeletons/FeedSkeleton.tsx`:
       - Refactor of FeedLoadingSkeleton.js using SkeletonBase primitives
       - Same constants: STORY_PHOTO_WIDTH=88, STORY_PHOTO_HEIGHT=130, STORY_BORDER_WIDTH=3, STORY_PROFILE_SIZE=32, FEED_PROFILE_SIZE=36
       - Same layout: horizontal ScrollView with 4 story card skeletons, then 2 feed card skeletons
       - Import useShimmer and SkeletonShape from SkeletonBase
       - Export as named export `FeedSkeleton`

    4. Create `src/components/skeletons/ConversationsSkeleton.tsx`:
       - 6 rows, each row: circle avatar (40px, layout.dimensions.avatarMedium) on left, two text lines on right (name: width 120px height 14px, message preview: width 200px height 12px)
       - Row padding: horizontal spacing.md (16px), vertical spacing.sm (12px)
       - Gap between avatar and text: spacing.sm (12px)

    5. Create `src/components/skeletons/FriendsSkeleton.tsx`:
       - 8 rows, each row: circle avatar (40px) on left, name text placeholder (width 140px height 14px) center, action button placeholder (width 70px height 30px borderRadius layout.borderRadius.md) on right
       - Row padding: horizontal spacing.md (16px), vertical spacing.xs (8px)

    6. Create `src/components/skeletons/CommentsSkeleton.tsx`:
       - 5 rows, each row: small circle avatar (32px, layout.dimensions.avatarSmall) on left, two text lines on right (username: width 100px height 12px, comment text: width '70%' height 14px)
       - Row padding: horizontal spacing.md (16px), vertical spacing.xs (8px)

    7. Create `src/components/skeletons/NotificationsSkeleton.tsx`:
       - 6 rows, each row: circle avatar (40px) on left, text block (two lines: width 180px height 14px, width 100px height 12px) in middle, small square thumbnail (40x40px, borderRadius layout.borderRadius.md) on right
       - Row padding: horizontal spacing.md (16px), vertical spacing.sm (12px)

    8. Create `__tests__/components/skeletons/SkeletonBase.test.tsx`:
       - Test useShimmer returns Animated.Value (use renderHook)
       - Test SkeletonShape renders with testID, has correct background color style
       - Snapshot or render tests for FeedSkeleton, ConversationsSkeleton, FriendsSkeleton, CommentsSkeleton, NotificationsSkeleton (each renders without crashing)
  </action>
  <verify>
    <automated>npm test -- __tests__/components/skeletons/ --bail</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "react-native-toast-message": "2.3.3"
    - src/components/skeletons/SkeletonBase.tsx contains "export function useShimmer" and "export function SkeletonShape"
    - src/components/skeletons/SkeletonBase.tsx contains "duration: 800" and "useNativeDriver: true"
    - src/components/skeletons/SkeletonBase.tsx contains "rgba(255, 255, 255, 0.1)"
    - src/components/skeletons/FeedSkeleton.tsx contains "STORY_PHOTO_WIDTH = 88" and "export function FeedSkeleton"
    - src/components/skeletons/ConversationsSkeleton.tsx contains "export function ConversationsSkeleton"
    - src/components/skeletons/FriendsSkeleton.tsx contains "export function FriendsSkeleton"
    - src/components/skeletons/CommentsSkeleton.tsx contains "export function CommentsSkeleton"
    - src/components/skeletons/NotificationsSkeleton.tsx contains "export function NotificationsSkeleton"
    - __tests__/components/skeletons/SkeletonBase.test.tsx exits 0
  </acceptance_criteria>
  <done>SkeletonBase primitive and first 5 skeleton screens created and tested. react-native-toast-message installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create remaining 4 skeleton screens + Toast config</name>
  <files>
    src/components/skeletons/AlbumsSkeleton.tsx,
    src/components/skeletons/DarkroomSkeleton.tsx,
    src/components/skeletons/ProfilePhotoGridSkeleton.tsx,
    src/components/skeletons/ActivitySkeleton.tsx,
    src/components/Toast.tsx
  </files>
  <read_first>
    src/components/skeletons/SkeletonBase.tsx,
    src/components/skeletons/ConversationsSkeleton.tsx,
    src/screens/DarkroomScreen.js,
    src/screens/ProfileScreen.js,
    src/screens/ActivityScreen.js,
    src/constants/colors.js,
    src/constants/spacing.js,
    src/constants/layout.js,
    src/constants/typography.js
  </read_first>
  <action>
    1. Create `src/components/skeletons/AlbumsSkeleton.tsx`:
       - 2-column grid of 6 album cells (3 rows x 2 cols)
       - Each cell: square placeholder with aspectRatio 1, borderRadius layout.borderRadius.md, gap spacing.xs (8px)
       - Container padding: spacing.md (16px)
       - Use useShimmer + SkeletonShape from SkeletonBase

    2. Create `src/components/skeletons/DarkroomSkeleton.tsx`:
       - 3 stacked card placeholders (simulating developing photo cards)
       - Each card: width '80%', aspectRatio 3/4, borderRadius layout.borderRadius.lg, centered
       - Stack with slight offset (absolute positioning, each card offset by 8px)
       - Use useShimmer + SkeletonShape from SkeletonBase

    3. Create `src/components/skeletons/ProfilePhotoGridSkeleton.tsx`:
       - 3-column grid of 9 cells (3 rows x 3 cols)
       - Each cell: square placeholder with aspectRatio 1, gap of 2px (matching typical photo grid style)
       - No padding on container (edge-to-edge grid)
       - Use useShimmer + SkeletonShape from SkeletonBase

    4. Create `src/components/skeletons/ActivitySkeleton.tsx`:
       - Same layout as NotificationsSkeleton (6 rows with avatar + text + thumbnail)
       - Activity and Notifications share the same row structure
       - Can re-export NotificationsSkeleton as ActivitySkeleton OR duplicate with minor adjustments if Activity screen has different row structure. Read ActivityScreen.js first to determine.

    5. Create `src/components/Toast.tsx`:
       - Import Toast and BaseToast from 'react-native-toast-message'
       - Export `toastConfig` object with single key `error`:
         - `style: { borderLeftColor: '#FF3333', borderLeftWidth: 4, backgroundColor: '#161628' }` (colors.status.danger, colors.background.secondary)
         - `text1Style: { color: '#E0E0F0', fontFamily: 'SpaceMono_400Regular', fontSize: 14 }` (colors.text.primary, typography body)
         - `text1NumberOfLines: 1`
       - Export component that renders `<ToastComponent config={toastConfig} position="bottom" bottomOffset={100} visibilityTime={2000} />`
       - Note: This component gets placed in App.js in plan 04 (screen wiring)
  </action>
  <verify>
    <automated>npm test -- __tests__/components/skeletons/ --bail</automated>
  </verify>
  <acceptance_criteria>
    - src/components/skeletons/AlbumsSkeleton.tsx contains "export function AlbumsSkeleton" and imports from "./SkeletonBase"
    - src/components/skeletons/DarkroomSkeleton.tsx contains "export function DarkroomSkeleton" and imports from "./SkeletonBase"
    - src/components/skeletons/ProfilePhotoGridSkeleton.tsx contains "export function ProfilePhotoGridSkeleton" and imports from "./SkeletonBase"
    - src/components/skeletons/ActivitySkeleton.tsx contains "export" and "ActivitySkeleton"
    - src/components/Toast.tsx contains "toastConfig" and "borderLeftColor" and "#FF3333"
    - src/components/Toast.tsx contains "bottomOffset" and "100" and "visibilityTime" and "2000"
    - src/components/Toast.tsx contains "SpaceMono_400Regular"
  </acceptance_criteria>
  <done>All 9 skeleton screens created. Toast config styled with pixel-art theme. All skeleton tests pass.</done>
</task>

</tasks>

<verification>
- `npm test -- __tests__/components/skeletons/ --bail` passes
- All 10 skeleton files exist in src/components/skeletons/
- Toast.tsx exists with correct styling constants
- react-native-toast-message is in package.json
</verification>

<success_criteria>
- 9 skeleton screen components exist matching exact layouts from real screens (per D-02)
- SkeletonBase extracts shimmer pattern from FeedLoadingSkeleton with 800ms loop, rgba(255,255,255,0.1) shimmer, colors.background.tertiary fill (per D-01, D-04)
- Toast config uses pixel art styling with bottom position, 100px offset, 2s visibility (per D-05)
- react-native-toast-message@2.3.3 installed
</success_criteria>

<output>
After completion, create `.planning/phases/19-performance-polish/19-01-SUMMARY.md`
</output>
