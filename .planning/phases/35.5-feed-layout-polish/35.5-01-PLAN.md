---
phase: 35.5-feed-layout-polish
plan: 01
type: execute
---

<objective>
Polish feed layout for seamless Instagram-style scrolling experience.

Purpose: Create cohesive visual flow from header → stories → feed cards without jarring section breaks.
Output: Edge-to-edge progress bar/emoji footer, removed "Feed" header, seamless stories→feed transition.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35.5-feed-layout-polish/35.5-CONTEXT.md
@.planning/phases/35.4-stories-feed-data-resume-fix/35.4-01-FIX-SUMMARY.md

**Key files:**
@src/screens/FeedScreen.js
@src/styles/PhotoDetailModal.styles.js

**Constraining decisions:**

- Phase 35.4-01-FIX: Progress bar horizontal padding matches emoji footer (16px) - we're changing both to 0
- Phase 35.2-01: Header height is 68px, tab bar is 88px - not changing these

**Context from /gsd:discuss-phase:**

- Instagram feed feel - seamless scroll where content flows without section breaks
- Edge-to-edge alignment for progress bar and emoji footer (screen edges, not photo edges)
- Visual consistency - progress bar, emoji footer, and photo edges all align to screen edges
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Remove "Feed" section header</name>
  <files>src/screens/FeedScreen.js</files>
  <action>
Remove the sectionHeader wrapper and "Feed" text from the ListHeaderComponent.

Current code (lines 629-633):

```jsx
{
  photos.length > 0 && (
    <View style={styles.sectionHeader}>
      <Text style={styles.sectionTitle}>Feed</Text>
    </View>
  );
}
```

Remove this entire conditional block. Also remove the sectionHeader and sectionTitle style definitions (lines 720-728) since they're now unused.
</action>
<verify>Run `npx expo start` and confirm stories flow directly into feed cards with no "Feed" label</verify>
<done>No "Feed" header visible, stories transition seamlessly to feed cards</done>
</task>

<task type="auto">
  <name>Task 2: Remove stories row border</name>
  <files>src/screens/FeedScreen.js</files>
  <action>
Remove the bottom border from storiesContainer style to create seamless transition.

Current style (lines 800-805):

```javascript
storiesContainer: {
  paddingVertical: 12,
  borderBottomWidth: 1,
  borderBottomColor: '#1A1A1A',
  backgroundColor: '#000000',
},
```

Remove the borderBottomWidth and borderBottomColor properties:

```javascript
storiesContainer: {
  paddingVertical: 12,
  backgroundColor: '#000000',
},
```

  </action>
  <verify>Visual inspection - no horizontal line between stories row and feed cards</verify>
  <done>Stories flow seamlessly into feed cards with no visual divider</done>
</task>

<task type="auto">
  <name>Task 3: Edge-to-edge progress bar and emoji footer</name>
  <files>src/styles/PhotoDetailModal.styles.js</files>
  <action>
Remove horizontal padding from progress bar and footer to make them edge-to-edge.

1. Update progressBarContainer (line 24-29):
   Change `paddingHorizontal: 16` to `paddingHorizontal: 0`

2. Update footer (line 121-126):
   Change `paddingHorizontal: 16` to `paddingHorizontal: 0`

This aligns progress bar and emoji footer with screen edges rather than photo edges.
</action>
<verify>Open stories modal and confirm progress bar and emoji pills extend to screen edges</verify>
<done>Progress bar and emoji footer align with screen edges (full-width)</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] No "Feed" header visible in feed
- [ ] No border/divider between stories and feed cards
- [ ] Progress bar in stories modal extends edge-to-edge
- [ ] Emoji footer in stories modal extends edge-to-edge
- [ ] Visual consistency across all three changes
- [ ] `npx expo start` runs without errors
</verification>

<success_criteria>

- All 3 tasks completed
- Feed scrolls seamlessly from header → stories → feed cards
- Stories modal UI is edge-to-edge aligned
- No TypeScript/lint errors
- Instagram-style cohesive scrolling experience
  </success_criteria>

<output>
After completion, create `.planning/phases/35.5-feed-layout-polish/35.5-01-SUMMARY.md`:

Use the summary template from `~/.claude/get-shit-done/templates/summary.md`
</output>
