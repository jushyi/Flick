---
phase: 11-add-video-support-to-main-camera
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - __tests__/hooks/useCameraBase.test.js
  - __tests__/services/uploadQueueService.test.js
  - __tests__/context/VideoMuteContext.test.js
autonomous: true
requirements: [VID-01, VID-03, VID-05, VID-10]

must_haves:
  truths:
    - "RED test scaffolds exist for hold-to-record gesture behavior"
    - "RED test scaffolds exist for video upload queue with mediaType/duration fields"
    - "RED test scaffolds exist for VideoMuteContext mute toggle and persistence"
  artifacts:
    - path: "__tests__/hooks/useCameraBase.test.js"
      provides: "Failing tests for hold-to-record, recording state, press handlers"
      contains: "handlePressIn"
    - path: "__tests__/services/uploadQueueService.test.js"
      provides: "Failing tests for video mediaType in addToQueue, video thumbnail generation"
      contains: "mediaType"
    - path: "__tests__/context/VideoMuteContext.test.js"
      provides: "Failing tests for mute toggle, initial muted state, setMuted"
      contains: "useVideoMute"
  key_links: []
---

<objective>
Create RED test scaffolds for the three Wave 0 gaps identified in RESEARCH.md: useCameraBase hold-to-record, uploadQueueService video fields, and VideoMuteContext mute state.

Purpose: These failing tests define the expected behavior before any implementation begins. Plans 02, 04, and 05/06 will make these tests GREEN. This satisfies the Nyquist rule -- every plan's verify can reference an automated test command.

Output: Three test files with failing (RED) tests that describe the video feature behaviors.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-add-video-support-to-main-camera/11-CONTEXT.md
@.planning/phases/11-add-video-support-to-main-camera/11-RESEARCH.md
@__tests__/setup/jest.setup.js
@src/hooks/useCameraBase.js
@src/services/uploadQueueService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RED test scaffold for useCameraBase hold-to-record</name>
  <files>__tests__/hooks/useCameraBase.test.js</files>
  <action>
Create `__tests__/hooks/useCameraBase.test.js` with failing test cases that describe the expected hold-to-record behavior. These tests will FAIL (RED) until Plan 11-04 implements the recording logic.

**Test cases to scaffold:**
1. `handlePressIn is exported from the hook` -- test that the hook returns handlePressIn function
2. `handlePressOut is exported from the hook` -- test that the hook returns handlePressOut function
3. `isRecording defaults to false` -- test initial state
4. `cameraMode defaults to 'picture'` -- test initial camera mode
5. `pressing and releasing quickly (tap) calls takePicture, not recordAsync` -- test tap behavior
6. `holding past HOLD_THRESHOLD_MS starts recording` -- test hold-to-record transition (use jest.useFakeTimers and advanceTimersByTime)
7. `releasing during recording stops recording and resets state` -- test release-during-recording
8. `MAX_RECORDING_DURATION is exported as 30` -- test constant

Use `renderHook` from `@testing-library/react-native` (or `@testing-library/react-hooks`). Mock `expo-camera`, `expo-haptics`, and the upload queue. Each test should have a clear `expect()` that will fail because the hook doesn't yet export these values/functions.

Mark all tests with `.skip` or use `test.todo()` for tests that can't be written yet without the implementation API. Alternatively, write the full test but expect it to fail -- the key is that the test FILE exists and describes the expected behavior.

**Preferred approach:** Write actual test code (not .todo) that imports from useCameraBase and checks for the new exports. These will throw/fail because the exports don't exist yet. Wrap in a describe block:
```javascript
describe('useCameraBase - video recording', () => {
  test('exports handlePressIn function', () => {
    // renderHook + check result.current.handlePressIn is a function
  });
  // ... etc
});
```
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const f = fs.readFileSync('__tests__/hooks/useCameraBase.test.js','utf8'); console.log('file exists:', true); console.log('has handlePressIn test:', f.includes('handlePressIn')); console.log('has isRecording test:', f.includes('isRecording')); console.log('has HOLD_THRESHOLD test:', f.includes('HOLD_THRESHOLD') || f.includes('hold'));"</automated>
    <manual>Verify test file exists with test cases for hold-to-record behavior. Tests should be RED (failing) at this point.</manual>
  </verify>
  <done>__tests__/hooks/useCameraBase.test.js exists with test cases covering handlePressIn, handlePressOut, isRecording, cameraMode, tap-vs-hold distinction, and MAX_RECORDING_DURATION. Tests are RED (failing) because the implementation doesn't exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Create RED test scaffolds for uploadQueueService video support and VideoMuteContext</name>
  <files>__tests__/services/uploadQueueService.test.js, __tests__/context/VideoMuteContext.test.js</files>
  <action>
**Part A: uploadQueueService video tests**

Create `__tests__/services/uploadQueueService.test.js` (or add to existing if it exists) with failing test cases for video upload queue behavior:

1. `addToQueue accepts mediaType parameter` -- call addToQueue with mediaType='video', verify the queue item has mediaType field
2. `addToQueue accepts duration parameter for videos` -- verify duration is stored in queue item
3. `video items call uploadVideo instead of uploadPhoto` -- mock both, verify uploadVideo is called for mediaType='video'
4. `video items generate thumbnail via generateVideoThumbnail` -- verify thumbnail generation is attempted for videos
5. `Firestore document includes mediaType, videoURL, duration fields for video items` -- verify the doc written to Firestore has these fields
6. `photo items continue to work as before (backward compatibility)` -- verify existing photo behavior unchanged

Mock firebase services, storage, etc. per existing test patterns in the project.

**Part B: VideoMuteContext tests**

Create `__tests__/context/VideoMuteContext.test.js` with failing test cases:

1. `isMuted defaults to true` -- render provider, check initial state
2. `toggleMute switches from muted to unmuted` -- call toggleMute, verify isMuted becomes false
3. `toggleMute switches back from unmuted to muted` -- call toggleMute twice, verify isMuted is true again
4. `setMuted(false) sets isMuted to false` -- call setMuted explicitly
5. `multiple consumers share the same state` -- render two consumers, verify they see the same isMuted value

Use `renderHook` with a wrapper that includes `VideoMuteProvider`. These tests will fail because VideoMuteContext.js doesn't exist yet (created in Plan 11-02 Task 2).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const f1 = fs.existsSync('__tests__/services/uploadQueueService.test.js'); const f2 = fs.existsSync('__tests__/context/VideoMuteContext.test.js'); console.log('uploadQueue test exists:', f1); console.log('VideoMuteContext test exists:', f2); if (f1) { const c1 = fs.readFileSync('__tests__/services/uploadQueueService.test.js','utf8'); console.log('has mediaType test:', c1.includes('mediaType')); } if (f2) { const c2 = fs.readFileSync('__tests__/context/VideoMuteContext.test.js','utf8'); console.log('has toggleMute test:', c2.includes('toggleMute')); }"</automated>
    <manual>Verify both test files exist with appropriate RED test cases.</manual>
  </verify>
  <done>__tests__/services/uploadQueueService.test.js has video-specific test cases (mediaType, duration, uploadVideo, thumbnail). __tests__/context/VideoMuteContext.test.js has mute state test cases (default muted, toggle, setMuted, shared state). All tests are RED (failing) because implementations don't exist yet.</done>
</task>

</tasks>

<verification>
- All three test files exist at expected paths
- Test files contain describe blocks with named test cases
- Tests are expected to FAIL (RED) at this point -- that is correct behavior
- `npx jest --testPathPattern="__tests__/(hooks/useCameraBase|services/uploadQueue|context/VideoMute)" --bail` should show failures (not crashes from missing files)
</verification>

<success_criteria>
RED test scaffolds exist for the three core video behaviors: hold-to-record gesture (useCameraBase), video upload queue (uploadQueueService), and global mute state (VideoMuteContext). These define the behavioral contract that subsequent plans will implement.
</success_criteria>

<output>
After completion, create `.planning/phases/11-add-video-support-to-main-camera/11-00-SUMMARY.md`
</output>
