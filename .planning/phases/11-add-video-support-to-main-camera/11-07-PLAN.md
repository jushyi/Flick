---
phase: 11-add-video-support-to-main-camera
plan: 07
type: execute
wave: 3
depends_on: [11-04, 11-05, 11-06]
files_modified: []
autonomous: false
requirements: [VID-01, VID-02, VID-03, VID-04, VID-05, VID-06, VID-07, VID-08, VID-09, VID-10]

must_haves:
  truths:
    - "All existing tests pass after video feature integration"
    - "Lint and format checks pass"
    - "The full video lifecycle works: capture -> develop -> reveal -> feed/stories"
  artifacts: []
  key_links: []
---

<objective>
Run full test suite, lint, and verify end-to-end video support readiness.

Purpose: After all video feature plans are implemented, verify that the full test suite passes, lint is clean, and the video lifecycle is ready for manual testing on a physical device after an EAS build.

Output: Green test suite, clean lint, verified code integration.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-add-video-support-to-main-camera/11-CONTEXT.md
@.planning/phases/11-add-video-support-to-main-camera/11-04-SUMMARY.md
@.planning/phases/11-add-video-support-to-main-camera/11-05-SUMMARY.md
@.planning/phases/11-add-video-support-to-main-camera/11-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and lint, fix any failures</name>
  <files></files>
  <action>
1. Run `npm test` to execute the full test suite. Fix any test failures caused by the video integration:
   - Missing mocks (expo-video, expo) should already be in jest.setup.js from Plan 01
   - Components importing VideoPlayer or useVideoMute should work with the mocked expo-video
   - uploadQueueService tests may need updates for the new mediaType parameter

2. Run `npm run lint` to check for lint errors. Fix any issues:
   - New imports should follow the project's import organization rules
   - No console.log (use logger instead)
   - Follow existing naming conventions

3. Run `npm run format` to ensure formatting is consistent.

4. If any tests fail, fix the underlying issues (not just the tests). Common failure points:
   - FeedPhotoCard tests may need to mock useVideoMute context
   - CameraScreen tests may need to handle the new recording state
   - uploadQueueService tests may need to handle the new addToQueue signature
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20 && npm run lint 2>&1 | tail -10</automated>
    <manual>All tests green, lint clean.</manual>
  </verify>
  <done>Full test suite passes with no failures. Lint is clean. All video feature code integrates without breaking existing functionality.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end video feature verification on physical device</name>
  <files></files>
  <action>
Present the user with the full verification checklist below. This requires a new EAS build because expo-video adds native modules and microphone permission was added. The user must:
1. Run `eas build --platform ios --profile production` (and/or Android)
2. Deploy storage rules: `firebase deploy --only storage`
3. After build completes, run `eas update --branch production --message "Phase 11: video support"`
4. Install the new build on a physical device and run through the test flow
  </action>
  <verify>User confirms all test steps pass</verify>
  <done>User has verified the full video lifecycle on a physical device: capture, develop, reveal, feed autoplay, PhotoDetail playback, stories auto-advance.</done>
  <what-built>Complete video support for the Flick camera app:
- Hold-to-record gesture (500ms threshold) with circular progress ring
- Video uploads to Firebase Storage with mediaType/duration/videoURL in Firestore
- Feed autoplay muted with viewport detection, tap to unmute (persists across videos)
- Duration badge on video cards
- PhotoDetail video playback with progress bar and mute toggle (loops in feed mode)
- Stories auto-advance after video plays to completion
- Darkroom video icon overlay
  </what-built>
  <how-to-verify>
**Before testing:** You must run a new EAS build because:
1. expo-video adds native modules
2. Microphone permission was added to expo-camera config

**Steps:**
1. Run `eas build --platform ios --profile production` (and/or Android)
2. After build completes, run `eas update --branch production --message "Phase 11: video support"` to push JS bundle
3. Install the new build on a physical device

**Test flow:**
1. Open camera tab
2. TAP the shutter button quickly — should take a photo (existing behavior preserved)
3. HOLD the shutter button for 1+ second — should see circular progress ring fill, recording should start with haptic feedback
4. Release to stop recording — should see haptic feedback, progress ring stops
5. Navigate to darkroom — video should appear with a small video icon overlay
6. Wait for developing then reveal
7. Check feed — video should autoplay muted when visible, pause when scrolled away
8. Tap video in feed — should unmute audio
9. Scroll to another video — should still be unmuted
10. Open PhotoDetail for a video — should play with progress bar and mute toggle, should loop
11. In stories view, play a video — should play once and auto-advance to next story
12. Deploy updated storage rules: `firebase deploy --only storage`
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found during testing</resume-signal>
</task>

</tasks>

<verification>
- All tests pass
- Lint is clean
- Manual verification on physical device confirms full video lifecycle
</verification>

<success_criteria>
The video feature is fully integrated, tested, and verified on a physical device. The full lifecycle works: capture via hold gesture, develop in darkroom, reveal, autoplay in feed, playback in PhotoDetail and stories.
</success_criteria>

<output>
After completion, create `.planning/phases/11-add-video-support-to-main-camera/11-07-SUMMARY.md`
</output>
