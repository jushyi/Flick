---
phase: 17-messaging-social
plan: 05
type: execute
wave: 4
depends_on: ["17-04"]
files_modified:
  - src/screens/MessagesListScreen.js
  - src/screens/ConversationScreen.js
  - src/screens/NewMessageScreen.js
  - src/screens/CameraScreen.js
autonomous: false
requirements: [MSG-10, MSG-11, MSG-03, MSG-06]

must_haves:
  truths:
    - "MessagesList screen renders conversations from useMessages hook (PowerSync)"
    - "Conversation screen renders messages from useConversation hook (TanStack + Realtime)"
    - "Screenshot detection inserts notification record into Supabase notifications table"
    - "Snap capture mode uses new snapService for upload"
    - "Tagged photo messages appear in conversation with correct rendering"
    - "Streak indicators display on conversation list and header using useStreaks"
  artifacts:
    - path: "src/screens/MessagesListScreen.js"
      provides: "Conversation list screen wired to useMessages + useStreakMap"
      contains: "useMessages"
    - path: "src/screens/ConversationScreen.js"
      provides: "Conversation screen wired to useConversation"
      contains: "useConversation"
    - path: "src/screens/CameraScreen.js"
      provides: "Camera snap mode wired to Supabase snapService"
      contains: "snapService"
    - path: "src/screens/NewMessageScreen.js"
      provides: "New message screen wired to Supabase getOrCreateConversation"
      contains: "getOrCreateConversation"
  key_links:
    - from: "src/screens/MessagesListScreen.js"
      to: "src/hooks/useMessages.ts"
      via: "import useMessages"
      pattern: "useMessages"
    - from: "src/screens/ConversationScreen.js"
      to: "src/hooks/useConversation.ts"
      via: "import useConversation"
      pattern: "useConversation"
    - from: "src/screens/ConversationScreen.js"
      to: "src/hooks/useStreaks.ts"
      via: "import useStreak for streak indicator"
      pattern: "useStreak"
    - from: "src/screens/CameraScreen.js"
      to: "src/services/supabase/snapService.ts"
      via: "import uploadAndSendSnap"
      pattern: "uploadAndSendSnap"
    - from: "src/screens/NewMessageScreen.js"
      to: "src/services/supabase/messageService.ts"
      via: "import getOrCreateConversation"
      pattern: "getOrCreateConversation"
---

<objective>
Wire the new Supabase hooks into the messaging screens: MessagesListScreen uses useMessages + useStreakMap, ConversationScreen uses useConversation + useStreak, CameraScreen snap mode uses snapService, NewMessageScreen uses getOrCreateConversation, and screenshot detection writes to Supabase notifications table. Then verify the full flow works.

Purpose: This is the integration layer -- connecting the services and hooks created in Plans 02-04 to the actual screens the user interacts with. No UI changes, just replacing Firebase data sources with Supabase ones.

Output: Updated screen files + human verification of full messaging flow
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-messaging-social/17-CONTEXT.md
@.planning/phases/17-messaging-social/17-02-SUMMARY.md
@.planning/phases/17-messaging-social/17-03-SUMMARY.md
@.planning/phases/17-messaging-social/17-04-SUMMARY.md

<interfaces>
From src/hooks/useMessages.ts (Plan 04):
```typescript
export const useMessages: () => {
  conversations: ConversationRow[],  // with otherUserId and unreadCount derived
  loading: boolean,
  deleteConversation: (conversationId: string) => Promise<void>,
}
```

From src/hooks/useConversation.ts (Plan 04):
```typescript
export const useConversation: (conversationId: string) => {
  messages: MessageRow[],
  isLoading: boolean,
  hasNextPage: boolean,
  fetchNextPage: () => void,
  isFetchingNextPage: boolean,
  sendMessage, sendReaction, removeReaction, sendReply,
  unsendMessage, deleteMessage, sendSnap, markSnapViewed, getSnapUrl, sendTaggedPhoto,
}
```

From src/hooks/useStreaks.ts (Plan 04):
```typescript
export const useStreak: (friendId: string) => { streak, state, dayCount, color, loading }
export const useStreakMap: (friendIds: string[]) => { streakMap: Map<string, {state, dayCount, color}>, loading }
```

From src/services/supabase/snapService.ts (Plan 03):
```typescript
export const uploadAndSendSnap: (conversationId: string, senderId: string, localUri: string, caption?: string | null) => Promise<{ messageId: string }>;
```

From src/services/supabase/messageService.ts (Plan 02):
```typescript
export const getOrCreateConversation: (currentUserId: string, friendId: string) => Promise<ConversationRow>;
export const generateConversationId: (userId1: string, userId2: string) => string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire MessagesListScreen and ConversationScreen to new hooks</name>
  <files>src/screens/MessagesListScreen.js, src/screens/ConversationScreen.js</files>
  <read_first>
    - src/screens/MessagesListScreen.js
    - src/screens/ConversationScreen.js
    - src/hooks/useMessages.ts
    - src/hooks/useConversation.ts
    - src/hooks/useStreaks.ts
  </read_first>
  <action>
**Part A: Update `src/screens/MessagesListScreen.js`:**

1. Replace Firebase hook import:
   - Remove: `import { useMessages } from '../hooks/useMessages';` (or wherever current Firebase hook is imported)
   - Add: `import { useMessages } from '../hooks/useMessages';` (same name, new .ts file auto-resolves)
   - Add: `import { useStreakMap } from '../hooks/useStreaks';`

2. Update hook usage:
   - The return shape from the new `useMessages` should match what the screen expects. Read the screen file carefully to see what properties it destructures from the current hook
   - Map any property name differences. Common mappings:
     - Old `conversations[i].otherUser` -> now derive from `otherUserId` by fetching profile separately or from existing friend data
     - Old `conversations[i].unread` -> now `conversations[i].unreadCount` (already derived in hook)
     - Old `conversations[i].lastMessage` -> now `{ text: c.last_message_text, type: c.last_message_type, senderId: c.last_message_sender_id, timestamp: c.last_message_at }`
   - The exact mappings depend on the current screen code -- read it first and adapt
   - Add `useStreakMap` call with friendIds derived from conversations' `otherUserId` values
   - Pass streak data to conversation list items for streak indicator rendering

3. Keep all existing UI/rendering logic unchanged. Only data source changes.

**Part B: Update `src/screens/ConversationScreen.js`:**

1. Replace Firebase hook import:
   - Remove old `useConversation` import from Firebase hooks
   - Add: `import { useConversation } from '../hooks/useConversation';`
   - Add: `import { useStreak } from '../hooks/useStreaks';`

2. Update hook usage:
   - Read the screen file to see current destructured properties
   - Map property name differences between old and new hook returns
   - Common mappings:
     - Old `loadMore()` -> now `fetchNextPage()`
     - Old `loading` -> now `isLoading`
     - Old `hasMore` -> now `hasNextPage`
   - Add `useStreak(otherUserId)` for streak indicator in conversation header

3. Update screenshot detection:
   - Find the existing screenshot detection code (uses `expo-screen-capture` listener)
   - Replace Firebase notification write with Supabase direct insert:
     ```typescript
     import { supabase } from '@/lib/supabase';
     // In screenshot handler:
     await supabase.from('notifications').insert({
       user_id: otherUserId,
       type: 'screenshot',
       from_user_id: currentUserId,
       data: { conversation_id: conversationId },
     });
     ```
   - Push notification delivery happens in Phase 18 -- just insert the record

4. Update message rendering for all 5 types:
   - The new `MessageRow` type uses snake_case from the database. The screen likely expects camelCase properties
   - Either map in the hook (preferred) or in the screen. If the hook already returns camelCase, no screen change needed
   - **text**: standard text rendering (no change needed)
   - **reaction**: emoji display on target message (no change needed if data shape matches)
   - **reply**: `reply_preview` JSONB contains the quoted context -- wire to reply bubble UI
   - **snap**: use `getSnapUrl` from useConversation to get signed URL for Polaroid display. Ensure ConversationScreen contains a `getSnapUrl` call for snap message rendering
   - **tagged_photo**: render tagged photo preview using `tagged_photo_id` reference. Ensure ConversationScreen contains a `tagged_photo_id` reference for tagged photo rendering

5. Keep all existing UI/rendering/animation logic unchanged. Only data source changes.
  </action>
  <verify>
    <automated>grep -c "useMessages\|useConversation\|useStreak\|useStreakMap" src/screens/MessagesListScreen.js src/screens/ConversationScreen.js</automated>
  </verify>
  <acceptance_criteria>
    - `MessagesListScreen.js` imports `useMessages` from `../hooks/useMessages` (not from Firebase path)
    - `MessagesListScreen.js` imports and uses `useStreakMap` from `../hooks/useStreaks`
    - `ConversationScreen.js` imports `useConversation` from `../hooks/useConversation` (not from Firebase path)
    - `ConversationScreen.js` imports and uses `useStreak` from `../hooks/useStreaks`
    - `ConversationScreen.js` contains screenshot notification insert to Supabase `notifications` table (not Firebase)
    - `ConversationScreen.js` contains `getSnapUrl` call for snap message rendering
    - `ConversationScreen.js` contains `tagged_photo_id` reference for tagged photo message rendering
    - No Firebase imports remain in either screen file (no `@react-native-firebase` imports)
    - All existing UI rendering code is preserved (no visual changes)
  </acceptance_criteria>
  <done>MessagesListScreen and ConversationScreen use new Supabase-backed hooks for all data operations, with all 5 message types rendering correctly</done>
</task>

<task type="auto">
  <name>Task 2: Wire CameraScreen snap mode and NewMessageScreen to Supabase services</name>
  <files>src/screens/CameraScreen.js, src/screens/NewMessageScreen.js</files>
  <read_first>
    - src/screens/CameraScreen.js
    - src/screens/NewMessageScreen.js
    - src/services/supabase/snapService.ts
    - src/services/supabase/messageService.ts
  </read_first>
  <action>
**Part A: Update `src/screens/CameraScreen.js` snap send path:**

1. Find the snap send path in CameraScreen. When mode='snap' (sending a photo as a snap to a friend), the camera currently calls Firebase snapService to upload and send. Replace this import:
   - Remove: any import of `uploadAndSendSnap` from Firebase `snapService` (e.g., `import { uploadAndSendSnap } from '../services/firebase/snapService'`)
   - Add: `import { uploadAndSendSnap } from '../services/supabase/snapService';`

2. The function signature is the same: `uploadAndSendSnap(conversationId, senderId, localUri, caption?)` returns `{ messageId }`. No call-site changes should be needed beyond the import path.

3. If CameraScreen also imports other Firebase snap functions (like `getSignedSnapUrl`), replace those too:
   - `import { getSignedSnapUrl } from '../services/supabase/snapService';`

4. Verify no Firebase snapService imports remain in CameraScreen.

**Part B: Update `src/screens/NewMessageScreen.js` conversation creation:**

1. Find the conversation creation code in NewMessageScreen. When a user taps a friend to start a new conversation, it currently calls Firebase messageService. Replace:
   - Remove: any import of `getOrCreateConversation` from Firebase `messageService`
   - Add: `import { getOrCreateConversation } from '../services/supabase/messageService';`

2. Also replace `generateConversationId` if imported:
   - `import { generateConversationId } from '../services/supabase/messageService';`

3. The function signatures are identical. No call-site changes needed beyond import paths.

4. Verify no Firebase messageService imports remain in NewMessageScreen.
  </action>
  <verify>
    <automated>grep -c "uploadAndSendSnap\|snapService\|getOrCreateConversation" src/screens/CameraScreen.js src/screens/NewMessageScreen.js</automated>
  </verify>
  <acceptance_criteria>
    - `CameraScreen.js` imports `uploadAndSendSnap` from `../services/supabase/snapService` (not from Firebase path)
    - `CameraScreen.js` contains no imports from `../services/firebase/snapService`
    - `NewMessageScreen.js` imports `getOrCreateConversation` from `../services/supabase/messageService` (not from Firebase path)
    - `NewMessageScreen.js` contains no imports from `../services/firebase/messageService`
    - No Firebase service imports remain in either screen file
  </acceptance_criteria>
  <done>CameraScreen snap mode uses Supabase snapService for upload and NewMessageScreen uses Supabase messageService for conversation creation</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full messaging flow</name>
  <files>src/screens/MessagesListScreen.js, src/screens/ConversationScreen.js, src/screens/CameraScreen.js, src/screens/NewMessageScreen.js</files>
  <action>
Human verification of the complete messaging system rewrite. All code changes are complete -- this task confirms the integration works end-to-end on a real device or simulator.
  </action>
  <what-built>
    Complete messaging system rewrite: SQL migrations (schema additions, 3 triggers), snap-cleanup Edge Function, messageService.ts, snapService.ts, streakService.ts, useMessages/useConversation/useStreaks hooks, and screen integration (MessagesListScreen, ConversationScreen, CameraScreen snap mode, NewMessageScreen). All Firebase messaging code replaced with Supabase equivalents.
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    1. Run `supabase db push` to apply the 2 new migrations
    2. Run `supabase functions deploy snap-cleanup` to deploy the Edge Function
    3. Start the dev server: `npx expo start`

    **Test flow:**
    1. Open Messages tab -- conversation list should load instantly (from PowerSync)
    2. Tap into a conversation -- messages should load (via TanStack query)
    3. Send a text message -- should appear immediately and show in conversation list
    4. Send a reaction (double-tap a message) -- heart/emoji should appear
    5. Send a reply (swipe on a message) -- quoted context should display
    6. Unsend a message (long press -> unsend) -- message should show as unsent
    7. Delete a message for yourself (long press -> delete for me) -- message should disappear
    8. Check unread counts -- open a conversation, go back, count should reset
    9. Check streak indicators -- streak badges should display on conversations with active streaks
    10. Send a snap from Camera (snap mode) -- should compress, upload via Supabase Storage, and display with Polaroid viewer
    11. Start a new conversation from NewMessageScreen -- should create conversation via Supabase
    12. Take a screenshot in a conversation -- notification record should be created in Supabase
    13. Verify tagged photo message renders with photo preview in conversation
  </how-to-verify>
  <verify>
    <automated>npm run lint 2>&1 | tail -3</automated>
  </verify>
  <done>Human confirms all 13 test flow items work correctly with no regressions from Firebase behavior</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run lint` passes on all modified screen files
- No Firebase imports remain in messaging-related screens
- All 5 message types render correctly (text, reaction, reply, snap, tagged_photo)
- Realtime updates work (messages appear without refresh)
- Streak indicators display on conversation list
- CameraScreen snap mode uploads via Supabase snapService
- NewMessageScreen creates conversations via Supabase messageService
</verification>

<success_criteria>
- Full messaging flow works end-to-end through Supabase
- All 5 message types send and display identically to current behavior
- Screenshot detection creates notification records in Supabase
- Streak indicators display correctly from PowerSync data
- CameraScreen snap send path uses Supabase snapService
- NewMessageScreen conversation creation uses Supabase messageService
- Human verification confirms no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/17-messaging-social/17-05-SUMMARY.md`
</output>
