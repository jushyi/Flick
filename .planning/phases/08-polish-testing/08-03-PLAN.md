---
phase: 08-polish-testing
plan: 03
type: execute
---

<objective>
Create app icon and splash screen assets for production-ready app appearance.

Purpose: Replace default Expo placeholders with branded assets required for App Store submission.
Output: Custom app icon (all required sizes) and configured splash screen.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**From CONCERNS.md:**
- App icon and splash screen: Using default Expo placeholders
- Current workaround: Default icons work but unprofessional
- Blocks: Can't submit to App Store without custom assets

**Current state:**
- Expo managed workflow (SDK ~54)
- app.json configures icon and splash
- Default Expo icon currently in use
- App branding: "LAPSE" - minimalist, black and white aesthetic

**Key files:**
@app.json
@assets/ (directory for icons and splash)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app icon assets</name>
  <files>assets/icon.png, assets/adaptive-icon.png, app.json</files>
  <action>
Create minimalist app icon matching Lapse branding:
1. Design: Simple "L" letterform or camera aperture icon on white background
2. Style: Black icon on white/light background (matches app aesthetic)
3. Format: PNG with transparency where appropriate

Create required icon files:
- assets/icon.png (1024x1024) - Main app icon for iOS
- assets/adaptive-icon.png (1024x1024) - Android adaptive icon foreground

If design tools not available, create a simple text-based icon:
- White square background
- Large bold "L" in center (black)
- Clean, minimal design

Update app.json icon configuration if needed:
- Verify "icon" path is correct
- Verify "android.adaptiveIcon" configuration

Note: For production, proper graphic design is recommended. This provides a professional placeholder that's better than Expo default.
  </action>
  <verify>
1. assets/icon.png exists and is 1024x1024
2. assets/adaptive-icon.png exists and is 1024x1024
3. Run `npx expo start` and verify icon appears in Expo Go app list
  </verify>
  <done>Custom app icon replaces Expo default, visible in app list and on device</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>App icon assets created and configured in app.json</what-built>
  <how-to-verify>
    1. Run: npx expo start
    2. Open Expo Go on iOS device
    3. Verify the app icon in Expo Go's recent projects list shows new icon (not default Expo icon)
    4. If on simulator: Check that app icon appearance is acceptable
    5. Confirm: Icon is visible and looks professional (not default placeholder)
  </how-to-verify>
  <resume-signal>Type "approved" if icon looks good, or describe changes needed</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Configure splash screen</name>
  <files>assets/splash.png, app.json</files>
  <action>
Create splash screen matching app branding:
1. Design: "LAPSE" text centered on light background
2. Style: Black text on off-white (#FAFAFA) background
3. Format: PNG, 1284x2778 (iPhone 14 Pro Max safe area)

Create splash screen:
- assets/splash.png with LAPSE branding
- Simple, clean design matching app aesthetic
- Text centered vertically and horizontally

Update app.json splash configuration:
- Set "splash.image" path
- Set "splash.resizeMode": "contain"
- Set "splash.backgroundColor": "#FAFAFA"

If design tools not available, create minimal text-based splash:
- #FAFAFA background (matches app)
- "LAPSE" text in bold black
  </action>
  <verify>
1. assets/splash.png exists
2. app.json splash configuration is correct
3. Run app and verify splash screen appears on launch (not default Expo splash)
  </verify>
  <done>Custom splash screen displays on app launch, matches app branding</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] assets/icon.png exists (1024x1024)
- [ ] assets/adaptive-icon.png exists (1024x1024)
- [ ] assets/splash.png exists
- [ ] app.json references all assets correctly
- [ ] Human verification: Icon visible in Expo Go
- [ ] Splash screen appears on app launch
</verification>

<success_criteria>

- Custom app icon replaces Expo default
- Custom splash screen displays on launch
- Assets match app branding (minimalist, black/white)
- Phase 8 complete, ready for TestFlight
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish-testing/08-03-SUMMARY.md`:

# Phase 8 Plan 03: Visual Assets Summary

**[Substantive one-liner]**

## Accomplishments

- Custom app icon created
- Splash screen configured
- Phase 8: Polish & Testing complete

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

Phase 8 complete. v1.2 Phone Authentication milestone ready for final testing and TestFlight distribution.
</output>
