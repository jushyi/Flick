---
phase: 21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration
plan: 04
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - .planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-UAT.md
  - src/**/*.ts
  - src/**/*.tsx
autonomous: false
requirements: [D-03, D-06, D-07, D-08, D-09]

must_haves:
  truths:
    - "A brand new user can sign up with a fresh phone number on the pure Supabase stack"
    - "All core journeys work with zero pre-existing data (fresh account)"
    - "Empty states display correctly when user has no photos, friends, messages, or albums"
    - "Both migrated data path AND fresh data path are verified (D-06 complete)"
  artifacts:
    - path: ".planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-UAT.md"
      provides: "Completed Part C with all fresh account test results"
  key_links:
    - from: "21-UAT.md Part C"
      to: "src/screens/ and src/services/supabase/"
      via: "Fresh account code paths (signup, empty states, first-time flows)"
      pattern: "result: pass"
---

<objective>
Test the fresh account data path per D-06. Create a brand new account on the pure Supabase stack (no migrated data) and verify core user journeys work from scratch. This catches issues where code assumes pre-existing data (migrated fields, legacy formats) that a new user would not have. Focus on: signup flow, empty states, first photo capture, first friend request, first message.

Purpose: Prove that new users signing up after the Supabase migration have a fully working experience, not just users with migrated Firebase data.
Output: Completed Part C of 21-UAT.md with all fresh account test cases showing `result: pass`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-CONTEXT.md
@.planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-RESEARCH.md
@.planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-UAT.md
@.planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Fresh account UAT — New signup + core journeys on clean data</name>
  <files>
    .planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-UAT.md
    src/**/*.ts
    src/**/*.tsx
  </files>
  <read_first>
    - .planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-UAT.md (current state with Parts A and B results)
    - .planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-02-SUMMARY.md (fixes from iOS UAT)
    - CLAUDE.md (auth flow: PhoneInputScreen -> VerificationScreen -> ProfileSetupScreen -> SelectsScreen -> ContactsSyncScreen -> NotificationPermissionScreen -> MainTabs)
  </read_first>
  <action>
    Interactive guided fresh account UAT. Tests the new user signup path and core flows with zero pre-existing data.

    **Part C test cases (Claude populates before starting if still placeholder):**

    **Fresh Signup Flow:**
    1. Open app logged out. PhoneInputScreen displays. Enter a NEW phone number (not used before in dev Supabase).
    2. OTP is sent via Twilio. Enter the code on VerificationScreen. Verification succeeds.
       - NOTE: If Twilio SMS delivery fails on dev (known risk from 21.1-CONTEXT.md), document as `blocked` with reason, and test remaining flows by manually creating the user in Supabase dashboard.
    3. ProfileSetupScreen appears (first-time user). Set username, display name, upload profile photo. Save succeeds.
    4. SelectsScreen appears. Pick 3 content preferences. Selections save.
    5. ContactsSyncScreen appears. Skip or allow contact sync. Screen transitions.
    6. NotificationPermissionScreen appears. Allow or skip. Screen transitions.
    7. MainTabs loads. Feed tab shows empty state (no friends, no photos).

    **Fresh Account -- Empty States:**
    8. Feed tab: Empty state component displays (not blank screen, not loading spinner stuck forever)
    9. Messages tab: Empty conversations list with appropriate messaging
    10. Profile tab: Profile shows the just-set username, display name, profile photo. No albums, no photos.
    11. Darkroom: No developing photos, empty state shown
    12. Activity/Notifications: Empty state shown
    13. Friends list: Empty with "add friends" prompt

    **Fresh Account -- First Actions:**
    14. Capture first photo. Camera opens, take photo, photo enters darkroom.
    15. Photo develops and reveals. Appears in own feed.
    16. Send friend request to the migrated test account. Request appears as pending.
    17. Accept friend request from the migrated account side. Both accounts now friends.
    18. Migrated account's photos appear in fresh account's feed.
    19. Fresh account sends first message to migrated account. Message delivers.
    20. Create first album from profile. Album appears in profile.

    **Fresh Account -- Data Integrity:**
    21. Close and reopen app. Session persists, cached data loads on cold start.
    22. Profile data matches what was entered during signup (no null fields, no legacy format issues).
    23. Photo URLs are CDN-backed permanent URLs (not Firebase Storage URLs).

    **Same fix-inline workflow as Plans 02/03.** When a test fails:
    - Claude identifies root cause
    - Claude writes fix, runs `npm test -- --bail`
    - Commits fix: `fix(21): [description] for fresh account path`
    - User reloads and re-tests

    **Platform:** Run on whichever platform is most convenient (iOS or Android). The goal is testing the DATA PATH, not platform behavior (already covered in Plans 02/03).

    **After all Part C test cases pass:**
    Update the Summary table with Part C counts. If ALL parts pass, update 21-UAT.md status to "COMPLETE - ALL PASS".
  </action>
  <verify>
    <automated>grep -c "result: pass" .planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Every test case in Part C of 21-UAT.md has `result: pass` (or `blocked` with documented reason for Twilio-only issues)
    - Fresh account signup completes through full onboarding flow (or blocker documented)
    - All empty state screens render correctly (no blank screens, no stuck spinners)
    - First photo, first friend request, first message all work for the fresh account
    - `npm test -- --bail` passes after any fixes
    - 21-UAT.md Summary table is fully updated with all section counts
    - If ALL test cases across Parts A, B, and C show pass: 21-UAT.md status updated to "COMPLETE - ALL PASS"
  </acceptance_criteria>
  <done>Fresh account data path fully verified. Both migrated and fresh account paths pass per D-06. 21-UAT.md is complete with final summary showing zero failures across all sections.</done>
</task>

</tasks>

<verification>
- Part C of 21-UAT.md has no `result: pending` entries
- 21-UAT.md Summary table shows totals for all 3 parts
- If all parts pass: 21-UAT.md status line says "COMPLETE"
- `npm test -- --bail` exits with code 0
</verification>

<success_criteria>
Both data paths verified per D-06: migrated Firebase data works (Parts A + B) and fresh Supabase-native accounts work (Part C). All empty states render correctly for new users. The UAT document is complete with every test case resolved. Phase 21 can be declared complete per D-07 (zero failures).
</success_criteria>

<output>
After completion, create `.planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-04-SUMMARY.md`
</output>
