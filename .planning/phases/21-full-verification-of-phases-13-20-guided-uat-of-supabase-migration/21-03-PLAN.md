---
phase: 21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - .planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-UAT.md
  - src/**/*.ts
  - src/**/*.tsx
autonomous: false
requirements: [D-02, D-04, D-07, D-08, D-09, D-10]

must_haves:
  truths:
    - "Every Android test case in Part B of 21-UAT.md has result: pass"
    - "Android-specific behaviors (back button, edge-to-edge, shadows, keyboard) are verified"
    - "All 7 user journeys complete successfully on Android"
    - "Platform-specific fixes are guarded with Platform.OS checks"
  artifacts:
    - path: ".planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-UAT.md"
      provides: "Completed Part B with all Android test results filled in"
  key_links:
    - from: "21-UAT.md Part B"
      to: "src/ screen and component files"
      via: "Android-specific inline fixes with Platform guards"
      pattern: "Platform\\.OS === 'android'"
---

<objective>
Interactive Android UAT walkthrough repeating all 7 user journeys plus Android-specific platform checks on an Android device/emulator against dev Supabase. Since Plan 02 already fixed TODO stubs on iOS, most flows should pass on Android. This plan focuses on catching Android-specific regressions: back button handling, edge-to-edge layout, shadow rendering, keyboard behavior, and responsive layout on different screen sizes.

Purpose: Verify the full Supabase migration works on Android with equal coverage per D-02. Fix Android-specific issues inline.
Output: Completed Part B of 21-UAT.md with all test cases showing `result: pass`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-CONTEXT.md
@.planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-RESEARCH.md
@.planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-UAT.md
@.planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Android UAT — All 7 journeys + Android-specific platform checks with inline fixes</name>
  <files>
    .planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-UAT.md
    src/**/*.ts
    src/**/*.tsx
  </files>
  <read_first>
    - .planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-UAT.md (current state with iOS results completed)
    - .planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-02-SUMMARY.md (iOS fixes applied -- know what was broken)
    - CLAUDE.md (Android-specific sections: Known Android Behavior Differences, Screen Size and Responsive Layout, Android-Specific app.json Fields)
  </read_first>
  <action>
    Interactive guided Android UAT session. Claude walks the user through Part B test cases on an Android device.

    Part B test cases mirror Part A journeys but add Android-specific checks. Claude populates Part B test cases before starting if they are still placeholder.

    **Android-specific test additions (beyond the 7 journeys):**

    1. **Back button behavior:** On every modal/overlay screen (PhotoDetail, Darkroom, ReportUser, HelpSupport), press the Android hardware back button. It should dismiss the modal, not navigate to an unexpected screen. Check BackHandler is properly set up.

    2. **Edge-to-edge layout:** With `edgeToEdgeEnabled: true`, verify:
       - Tab bar does not overlap with the gesture navigation bar
       - Bottom sheets and fixed footers have proper bottom padding from `useSafeAreaInsets()`
       - Status bar content is visible (not hidden behind camera punch-hole)

    3. **Shadow rendering:** Verify cards/buttons that use shadows on iOS use `elevation` on Android. Check: feed photo cards, profile cards, modal overlays, action buttons.

    4. **Keyboard behavior:** On screens with text input (comment entry, message composer, profile edit, search):
       - `KeyboardAvoidingView` uses `behavior="height"` on Android (not "padding")
       - Input field stays visible above keyboard
       - Keyboard dismisses when tapping outside

    5. **Font rendering:** Pixel art fonts render correctly. No text clipping or misalignment on Android.

    6. **Camera screen:** Camera preview fills the screen correctly on both 16:9 and 20:9 aspect ratio devices. `CAMERA_HEIGHT` from `useCamera()` calculates correctly.

    **Fix patterns for Android issues:**
    ```typescript
    // Platform-specific style
    import { Platform } from 'react-native';
    const styles = StyleSheet.create({
      card: {
        ...Platform.select({
          ios: { shadowColor: '#000', shadowOffset: { width: 0, height: 2 }, shadowOpacity: 0.1, shadowRadius: 4 },
          android: { elevation: 4 },
        }),
      },
    });

    // Keyboard behavior
    <KeyboardAvoidingView behavior={Platform.select({ ios: 'padding', android: 'height' })} />

    // Back button handler
    useFocusEffect(
      useCallback(() => {
        const sub = BackHandler.addEventListener('hardwareBackPress', () => { navigation.goBack(); return true; });
        return () => sub.remove();
      }, [navigation])
    );
    ```

    All fixes MUST use Platform guards -- never apply Android-only styles unconditionally.
    After each fix: run `npm test -- --bail`, commit with `fix(21): [description] android`, user reloads and re-tests.
    After all Part B test cases pass: update the Summary table.
  </action>
  <verify>
    <automated>grep -c "result: pass" .planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Every test case in Part B of 21-UAT.md has `result: pass`
    - All Android-specific fixes use `Platform.OS === 'android'` or `Platform.select()` guards
    - `npm test -- --bail` passes after all fixes
    - 21-UAT.md Summary table shows Android pass counts
    - No unconditional Android-only styles exist in fixed files (grep for `elevation:` without Platform guard returns only pre-existing code)
  </acceptance_criteria>
  <done>All Android UAT test cases pass on device. Android-specific issues are fixed with proper platform guards. Part B of 21-UAT.md is complete with zero failures.</done>
</task>

</tasks>

<verification>
- `grep -c "result: pass"` in Part B section matches total Part B test case count
- `grep -c "result: fail\|result: pending"` in Part B section returns 0
- `npm test -- --bail` exits with code 0
- All Android fix commits use Platform guards
</verification>

<success_criteria>
Every test case in Part B (Android UAT with migrated account) of 21-UAT.md shows `result: pass`. Android-specific platform behaviors are verified and any issues are fixed with proper Platform guards. The automated test suite still passes.
</success_criteria>

<output>
After completion, create `.planning/phases/21-full-verification-of-phases-13-20-guided-uat-of-supabase-migration/21-03-SUMMARY.md`
</output>
