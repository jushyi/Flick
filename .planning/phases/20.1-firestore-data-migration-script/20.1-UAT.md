---
status: complete
phase: 20.1-firestore-data-migration-script
source: 20.1-01-SUMMARY.md, 20.1-02-SUMMARY.md, 20.1-03-SUMMARY.md
started: 2026-03-26T00:00:00Z
updated: 2026-03-26T14:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Migration Script CLI Help
expected: Run `npx tsx scripts/migrate-firestore-data.ts --help` and it shows usage info including --env, --dry-run, --collection flags, and lists all 17 collection names.
result: pass

### 2. Migration Script Dry Run
expected: Run `npx tsx scripts/migrate-firestore-data.ts --env dev --dry-run` and it connects to Firestore, reads data, and logs what WOULD be migrated without actually inserting into Supabase. Each collection shows count of documents found.
result: pass

### 3. Migration Script Single Collection
expected: Run `npx tsx scripts/migrate-firestore-data.ts --env dev --dry-run --collection users` and it only processes the users collection, skipping all others.
result: pass

### 4. Migration Script Progress/Resume
expected: After running a migration (even dry-run), a `.data-migration-progress.json` file is created tracking per-collection status (pending/complete/error). On re-run, completed collections are skipped.
result: pass

### 5. Migration Script FK Ordering
expected: Collections migrate in strict FK dependency order: users -> photos -> photo_reactions -> photo_tags -> viewed_photos -> friendships -> blocks -> reports -> support_requests -> conversations -> messages -> message_deletions -> streaks -> comments -> comment_likes -> albums -> album_photos.
result: pass

### 6. Verification Script Runs
expected: Run `npx tsx scripts/verify-migration-cross-db.ts --env dev` and it connects to both Firestore and Supabase, compares document/row counts for all 17 collections, and prints a pass/fail summary.
result: pass

### 7. Verification Script Per-User Check
expected: Run `npx tsx scripts/verify-migration-cross-db.ts --env dev --user <firebase_uid>` and it shows field-by-field comparison for that user, per-user data counts, and sample photo comparison.
result: pass

### 8. Migration Orchestrator Script
expected: Run `npx tsx scripts/run-migration-cycle.ts --help` and it shows the 5-step pipeline with --skip-* flags.
result: pass

### 9. Environment Templates Exist
expected: `.env.dev.example` and `.env.prod.example` exist in the project root with all required environment variables. Prod template includes safety warnings.
result: pass

## Summary

total: 9
passed: 9
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]
