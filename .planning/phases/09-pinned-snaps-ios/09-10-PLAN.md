---
phase: 09-pinned-snaps-ios
plan: 10
type: execute
wave: 3
depends_on: ["09-09"]
files_modified: []
autonomous: false
requirements:
  - PINI-01
  - PINI-02
  - PINI-03
  - PINI-04
  - PINI-05

must_haves:
  truths:
    - "Sending a pinned snap from Sender device results in Recipient seeing a rich notification with thumbnail image on lock screen"
    - "Tapping the notification opens the app to the correct conversation"
    - "Viewing the snap in SnapViewer dismisses the notification from Notification Center"
    - "If recipient swipes away the notification and re-opens the app, the notification reappears"
    - "Pin toggle on send screen works correctly and remembers preference per friend"
  artifacts: []
  key_links:
    - from: "SnapPreviewScreen (sender)"
      to: "Cloud Function → Push Notification → NSE → Notification Center (recipient)"
      via: "End-to-end pinned snap delivery pipeline"
      pattern: "pinned snap flow"
---

<objective>
End-to-end verification of the complete pinned snap notification flow on two physical iOS devices after an EAS native build. This replaces the old Plan 09-05 checkpoint which was designed for the Live Activity approach.

Purpose: The pivot from Live Activities to persistent notifications changes the recipient-side behavior entirely. All JS logic and native NSE code must be verified working together on real hardware with real push notifications.

Output: Verified working pinned snap feature or a list of issues to fix.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pinned-snaps-ios/09-CONTEXT.md
@.planning/phases/09-pinned-snaps-ios/09-RESEARCH.md
@.planning/phases/09-pinned-snaps-ios/09-08-SUMMARY.md
@.planning/phases/09-pinned-snaps-ios/09-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight automated checks and Cloud Function deployment reminder</name>
  <files></files>
  <action>
    Run the full automated verification suite before the human verification step:

    1. **Full test suite:** `npm test` -- all tests must pass
    2. **Lint check:** `npm run lint` -- zero errors
    3. **Grep for stale references:** Verify zero references to:
       - `liveActivityService` in any source file (excluding .planning/ docs)
       - `startPinnedSnapActivity` or `endPinnedSnapActivity` in any source file
       - `ActivityKit` in any file under targets/ or modules/
       - `PinnedSnapAttributes` in any file under targets/ or modules/
    4. **Verify deleted directories:** Confirm `targets/FlickLiveActivity/` and `modules/live-activity-manager/` do not exist
    5. **Verify pinnedNotificationService exists:** Confirm `src/services/pinnedNotificationService.js` exists and exports dismissPinnedSnapNotification, checkAndRedeliverPinnedSnaps, trackPinnedSnap, clearPinnedSnap
    6. **Verify NSE is simplified:** Confirm `targets/FlickNotificationService/NotificationService.swift` contains `UNNotificationAttachment` and does NOT contain `ActivityKit` or `Activity<`

    Output a summary of all checks. If any fail, flag them for investigation before proceeding to the human checkpoint.

    **Remind the user:**
    - Run `eas build --platform ios --profile production` (or appropriate profile) to create a native build with the simplified NSE
    - Run `firebase deploy --only functions` to deploy the Cloud Function changes (mutableContent flag, pinned notification payload)
    - Both are required before the physical device test
  </action>
  <verify>
    <automated>
      bash -c "npm test 2>&1 | tail -5 && npm run lint 2>&1 | tail -3 && echo '=== Stale reference check ===' && ! grep -r 'liveActivityService' --include='*.js' --include='*.ts' --include='*.swift' App.js src/ targets/ modules/ 2>/dev/null && echo 'PASS: No liveActivityService refs' && ! grep -r 'ActivityKit' targets/ modules/ 2>/dev/null && echo 'PASS: No ActivityKit refs' && test ! -d targets/FlickLiveActivity && echo 'PASS: FlickLiveActivity gone' && test ! -d modules/live-activity-manager && echo 'PASS: live-activity-manager gone' && test -f src/services/pinnedNotificationService.js && echo 'PASS: pinnedNotificationService exists' && grep 'UNNotificationAttachment' targets/FlickNotificationService/NotificationService.swift && echo 'PASS: NSE uses UNNotificationAttachment' && echo 'ALL PRE-FLIGHT CHECKS PASSED'"
    </automated>
    <sampling_rate>Run before proceeding to checkpoint</sampling_rate>
  </verify>
  <done>All automated checks pass. User is reminded about EAS build and Cloud Function deployment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification on physical iOS devices</name>
  <files></files>
  <action>
    Human verification of the complete pinned snap notification flow on two physical iOS devices.

    What was built:
    - Sender: Pin toggle on SnapPreviewScreen with per-friend sticky preference
    - Cloud Function: Sends pinned notification with mutableContent flag and thumbnail URL
    - NSE: Downloads thumbnail and attaches as rich notification image
    - Recipient: Sees persistent notification with thumbnail on lock screen
    - Dismissal: Viewing snap in SnapViewer dismisses the notification
    - Re-delivery: Swiped-away notifications re-appear on next app foreground
    - Deep link: Tapping notification opens correct conversation

    Prerequisites:
    1. New EAS build installed on two physical iOS devices (Sender + Recipient)
    2. Cloud Functions deployed (firebase deploy --only functions)
    3. Both users logged in with push notification permission granted

    Test 1 - Basic pinned snap delivery: Send pinned snap from Sender. Recipient sees rich notification with thumbnail on lock screen.
    Test 2 - Tap to open: Tap notification opens correct conversation.
    Test 3 - Dismissal on view: View snap in app, notification disappears from Notification Center.
    Test 4 - Re-delivery: Swipe away notification, open app, notification reappears after 3-4 seconds.
    Test 5 - Multiple pins: Send 3 pinned snaps, Recipient sees 3 stacked notifications.
    Test 6 - Control: Non-pinned snap gets standard notification without thumbnail.
    Test 7 - Toggle memory: Pin preference persists per-friend across snap sessions.
  </action>
  <verify>
    <manual>All 7 test scenarios pass on physical iOS devices</manual>
  </verify>
  <done>All 7 end-to-end test scenarios verified on physical iOS devices. Pinned snap notification feature is confirmed working.</done>
  <resume-signal>
    Type "approved" if all tests pass, or describe specific failures with test numbers for targeted fixes
  </resume-signal>
</task>

</tasks>

<verification>
1. All automated pre-flight checks pass (tests, lint, stale references, file structure)
2. Human verification of 7 test scenarios on physical iOS devices
3. Pin toggle, notification delivery, thumbnail display, tap-to-open, dismissal, re-delivery all working
</verification>

<success_criteria>
- Full pinned snap flow works end-to-end on physical iOS devices
- Rich notification with thumbnail displays on recipient lock screen
- Notification dismissed after snap viewed
- Swiped-away notification re-delivered on app foreground
- Pin toggle remembers preference per friend
- Non-pinned snaps unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/09-pinned-snaps-ios/09-10-SUMMARY.md`
</output>
