---
phase: 20-typescript-sweep-firebase-removal
plan: 04
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/hooks/*.ts (17 files renamed from .js, minus those already .ts)
autonomous: true
requirements:
  - TS-02

must_haves:
  truths:
    - "All hook files are TypeScript with typed parameters and return values"
    - "No .js files remain in src/hooks/"
    - "Zero any types in hook files"
    - "Per D-12: unnecessary comments stripped"
  artifacts:
    - path: "src/hooks/useCamera.ios.ts"
      provides: "iOS camera hook with typed return value"
    - path: "src/hooks/useCamera.android.ts"
      provides: "Android camera hook with typed return value"
    - path: "src/hooks/useCameraBase.ts"
      provides: "Shared camera logic with typed constants and functions"
  key_links:
    - from: "src/hooks/useCamera.ios.ts"
      to: "src/types/hooks.ts"
      via: "imports UseCameraReturn type"
      pattern: "import.*UseCameraReturn.*from.*types/hooks"
---

<objective>
Convert all remaining JavaScript hook files to TypeScript with full type annotations.

Purpose: Hooks are the business logic layer between context/services and components. Typing them provides compile-time safety for all data flowing to the UI.
Output: All 17 hook JS files converted to .ts with typed parameters, returns, and zero `any` types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-typescript-sweep-firebase-removal/20-CONTEXT.md
@.planning/phases/20-typescript-sweep-firebase-removal/20-01-SUMMARY.md

@src/types/hooks.ts
@src/types/services.ts
@src/types/common.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert camera and core interaction hooks to TypeScript</name>
  <files>
    src/hooks/useCamera.ios.ts (renamed from .js)
    src/hooks/useCamera.android.ts (renamed from .js)
    src/hooks/useCameraBase.ts (renamed from .js)
    src/hooks/useSwipeableCard.ts (renamed from .js)
    src/hooks/usePhotoDetailModal.ts (renamed from .js)
    src/hooks/useViewedStories.ts (renamed from .js)
    src/hooks/useScreenshotDetection.ts (renamed from .js)
    src/hooks/usePinPreference.ts (renamed from .js)
  </files>
  <read_first>
    src/hooks/useCamera.ios.js
    src/hooks/useCamera.android.js
    src/hooks/useCameraBase.js
    src/hooks/useSwipeableCard.js
    src/hooks/usePhotoDetailModal.js
    src/hooks/useViewedStories.js
    src/hooks/useScreenshotDetection.js
    src/hooks/usePinPreference.js
    src/types/hooks.ts
    src/types/services.ts
  </read_first>
  <action>
Convert 8 hook files from .js to .ts. For each file:

1. Rename `.js` to `.ts`
2. Add type annotations to all function parameters, return values, state variables, refs, and callbacks
3. Per D-02: Zero `any` types -- use proper types or `unknown` with type guards
4. Per D-12: Strip comments that restate the code
5. Import types from `@/types/hooks.ts` and `@/types/services.ts` where applicable
6. Use the `UseCameraReturn` type from `@/types/hooks.ts` for camera hook return values

**useCamera.ios.ts and useCamera.android.ts:**
These are platform-specific files. Read each carefully -- they share `useCameraBase` but have platform differences.
- Type the camera ref: `React.RefObject<CameraView>` (from expo-camera)
- Type flash/facing/zoom state
- Type capture and recording functions with explicit return types
- Export layout constants with `as const`

**useCameraBase.ts:**
- Type shared camera logic (permissions, queue integration)
- Type exported constants: `TAB_BAR_HEIGHT: number`, `FOOTER_HEIGHT: number`, `CAMERA_HEIGHT: number`

**useSwipeableCard.ts:**
- Type the gesture handler callbacks
- Type SharedValue from reanimated: `SharedValue<number>` for translateX, translateY
- Type the return value explicitly

**usePhotoDetailModal.ts:**
- Type the modal state and navigation functions
- Import `FeedPhoto` from `@/types/services`

**useViewedStories.ts:**
- Type the viewed stories set/map structure
- Type the check/mark functions

**useScreenshotDetection.ts:**
- Type the detection callback and event handler

**usePinPreference.ts:**
- Type the preference state and toggle function

After all conversions, verify these hooks compile:
```bash
npx tsc --noEmit
```
  </action>
  <verify>
    <automated>bash -c "for f in useCamera.ios useCamera.android useCameraBase useSwipeableCard usePhotoDetailModal useViewedStories useScreenshotDetection usePinPreference; do test -f src/hooks/$f.ts || echo \"MISSING: $f.ts\"; done && npx tsc --noEmit 2>&1 | tail -3 && echo PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useCamera.ios.ts` exists (no .js)
    - `src/hooks/useCamera.android.ts` exists (no .js)
    - `src/hooks/useCameraBase.ts` exists (no .js)
    - `src/hooks/useSwipeableCard.ts` exists (no .js)
    - `src/hooks/usePhotoDetailModal.ts` exists (no .js)
    - `src/hooks/useViewedStories.ts` exists (no .js)
    - `src/hooks/useScreenshotDetection.ts` exists (no .js)
    - `src/hooks/usePinPreference.ts` exists (no .js)
    - `grep -r ": any" src/hooks/useCamera* src/hooks/useSwipeable* src/hooks/usePhotoDetail* src/hooks/useViewed* src/hooks/useScreenshot* src/hooks/usePin*` returns 0 results
  </acceptance_criteria>
  <done>All 8 camera and core interaction hooks converted to TypeScript with typed parameters, returns, and zero `any` types.</done>
</task>

<task type="auto">
  <name>Task 2: Convert messaging and social hooks to TypeScript</name>
  <files>
    src/hooks/useConversation.ts (renamed from .js -- note: may already have .ts from Phase 17)
    src/hooks/useMessages.ts (renamed from .js -- note: may already have .ts from Phase 17)
    src/hooks/useMessageActions.ts (renamed from .js)
    src/hooks/useStreaks.ts (renamed from .js -- note: may already have .ts)
    src/hooks/useComments.js (check if .ts exists already)
    src/hooks/useDarkroom.js (check if .ts exists already)
    src/hooks/useFeedPhotos.js (check if .ts exists already)
    src/hooks/useMentionSuggestions.js (check if .ts exists already)
  </files>
  <read_first>
    src/hooks/useConversation.js
    src/hooks/useMessages.js
    src/hooks/useMessageActions.js
    src/hooks/useStreaks.js
    src/types/services.ts
    src/types/hooks.ts
  </read_first>
  <action>
Convert remaining messaging and social hook files from .js to .ts.

IMPORTANT: Several hooks may already have .ts versions from Phases 15-17 (useComments.ts, useDarkroom.ts, useFeedPhotos.ts, useMentionSuggestions.ts, useStreaks.ts). Before converting:

1. Run `ls src/hooks/` to see which files are already .ts
2. For hooks that have BOTH .js and .ts versions: delete the .js version (the .ts version is the Supabase rewrite)
3. For hooks that only have .js: convert to .ts

**Hooks likely needing conversion (only .js exists):**

`src/hooks/useConversation.js` -> `.ts`:
- Type the conversation state, message list, pagination cursor
- Import `Conversation`, `Message` from `@/types/services`
- Type the send/load/subscribe functions

`src/hooks/useMessages.js` -> `.ts`:
- Type conversation list with unread counts
- Type friend data join
- Import types from `@/types/services`

`src/hooks/useMessageActions.js` -> `.ts`:
- Type reaction, reply, unsend, delete actions
- Type the action handlers with explicit parameter and return types

`src/hooks/useStreaks.js` -> `.ts` (if not already converted):
- Type streak state derivation (active, expiring, warning)
- Type timer values

For ALL hooks:
- Per D-02: Zero `any` types
- Per D-12: Strip unnecessary comments
- If hook still imports from Firebase services, update to import from Supabase services
- Type all useEffect dependencies, useCallback parameters, useState generics
- Type all ref generics: `useRef<Type>(initialValue)`

After conversion, verify NO .js hook files remain:
```bash
find src/hooks -name "*.js"
# Must return 0 files
```
  </action>
  <verify>
    <automated>bash -c "JS_COUNT=$(find src/hooks -name '*.js' 2>/dev/null | wc -l) && echo \"Remaining JS hook files: $JS_COUNT\" && test $JS_COUNT -eq 0 && npx tsc --noEmit 2>&1 | tail -3 && echo PASS || echo FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `find src/hooks -name "*.js"` returns 0 files
    - `src/hooks/useConversation.ts` exists
    - `src/hooks/useMessages.ts` exists
    - `src/hooks/useMessageActions.ts` exists
    - `grep -r ": any" src/hooks/ --include="*.ts"` returns 0 results
    - `grep -r "@react-native-firebase" src/hooks/ --include="*.ts"` returns 0 results
    - `npx tsc --noEmit` passes for all hook files
  </acceptance_criteria>
  <done>All hook files are TypeScript. Zero .js files remain in src/hooks/. All hooks have typed parameters, returns, and state. No Firebase imports remain.</done>
</task>

</tasks>

<verification>
- `find src/hooks -name "*.js" | wc -l` returns 0
- `npx tsc --noEmit` passes
- `grep -r ": any" src/hooks/ --include="*.ts" | wc -l` returns 0
- `grep -r "@react-native-firebase" src/hooks/ --include="*.ts" | wc -l` returns 0
- All hooks export typed functions with explicit return types
</verification>

<success_criteria>
All hooks in src/hooks/ are TypeScript with full type annotations, zero `any` types, and zero Firebase imports.
</success_criteria>

<output>
After completion, create `.planning/phases/20-typescript-sweep-firebase-removal/20-04-SUMMARY.md`
</output>
