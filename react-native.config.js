// React Native config for auto-linking native modules
// This ensures @react-native-firebase packages are properly linked
module.exports = {
  // Empty config allows default auto-linking behavior
  // @react-native-firebase/app and @react-native-firebase/auth
  // will be automatically detected and linked
};
