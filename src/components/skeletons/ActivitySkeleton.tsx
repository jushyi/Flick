/**
 * Skeleton loading state for the Activity screen.
 * Activity and Notifications share the same row structure
 * (avatar + text lines + thumbnail), so we re-export NotificationsSkeleton.
 */
export { NotificationsSkeleton as ActivitySkeleton } from './NotificationsSkeleton';
